# cw-standwalk-stance-mesh2-stage2-dualbc1-anchor9-gradnorm

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-27T12:54:11+00:00

**pod**: hexapod-mjx-train-1

**steps**: 2000000

**parent**: cw-standwalk-stance-mesh2-stage2-dualbc1-anchor8-advstats

**wandb_id**: 27lym34j

**hypothesis**: Plain sentence: check whether PPO's own single global gradient-norm clip (not the BC anchor, not log_std, not the trunk, not advantage normalization -- all four now refuted/ruled out) is the shared mechanism quietly starving one dual-core branch's learning even though its GRU/heads/critic are already fully separate. anchor8-advstats REFUTED advantage-normalization-scope on BOTH seeds (adv_loco_std is 2-5x LARGER than adv_stance_std for most of training, the opposite of predicted) -- this run is the next fallback named in anchor8's own hypothesis text: sb3-contrib's RecurrentPPO.train() calls ONE clip_grad_norm_(self.policy.parameters(), max_grad_norm) over ALL params together per minibatch; a code read confirms the dual-core policy has fully separate core_a/core_b GRUs, action_net/action_net_b, value_net/value_net_b -- the only shared computation left is a parameterless FlattenExtractor -- so if per-core RAW pre-clip grad norms are lopsided, the ONE shared clip factor still rescales both cores' otherwise-independent gradients together, which could reproduce exactly the observed reward-trough-then-partial-recovery shape without any shared weights at all. Read-only diagnostic (train.bc_anchor_debug_gradnorm=1, zero training effect -- 5 new unit tests, default-off bit-exact incl. exact clip_grad_norm_ function-object restoration) on top of the IDENTICAL anchor8-advstats recipe (same seed, same init-from anchor2, same everything except the diagnostic flag swap) -- same known clean-walk seed0 trajectory, bit-for-bit reproducible since the new diagnostic changes no gradient/weight/RNG draw. Logs train/gradnorm_{a,b,shared}_mean (mean per-core pre-clip grad L2 norm across the train() call's minibatches). Prediction-if-true: gradnorm_a_mean (loco) and gradnorm_b_mean (stance) diverge sharply (one several-x the other) especially during the reward-trough window (rollout idx 6-13 in anchor8's read), i.e. whichever core's norm dominates is disproportionately clipped alongside the other. Prediction-if-false: the two stay comparable throughout -- rules out grad-norm-clip coupling too, at which point every proposed shared-computation mechanism in the dual-core design will have been tested and refuted, and the next candidate becomes reward/task-level (is the WALK REWARD ITSELF simply harder to optimize under this recipe's exact goal-mix/DR schedule, independent of any cross-mode interference).

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. DIAGNOSTIC-ONLY: bc_anchor_debug_gradnorm is read-only logging, zero training effect, no behavioral eval read required to close -- deliverable is the train/gradnorm_a_mean vs train/gradnorm_b_mean W&B history trace (pair by call-order within the run, NOT by matching wandb _step -- tensorboard-sync gives every train/* scalar including stock PPO ones its own separate _step; see anchor8-advstats verdict for the exact pitfall). WIRING CHECK FIRST: confirm nonzero train/gradnorm_a_mean AND train/gradnorm_b_mean entries exist in the cached history (proves the dual-core branch of _gradnorm_diag_ctx fired, not the non-dual-policy skip path). Read: if gradnorm_a_mean is persistently several-x gradnorm_b_mean (or vice versa) especially during the reward-trough window (rollout idx ~6-13, matching anchor8's own trough), the shared-clip-coupling hypothesis is SUPPORTED -- design a per-core gradient clip (default off, bit-exact) as the next mechanism arm. If the two stay comparable throughout, REFUTED -- every proposed shared-computation coupling mechanism (log_std, trunk-gradient, advantage-normalization, grad-norm-clip) has now been tested and refuted; escalate to a reward/task-level audit of the walk objective under this exact recipe, not another architecture-sharing arm.

**verdict**: CANARY FAIL - MECHANISM: MECHANISM-HEALTH CANARY, seed0 own scope: WIRING CHECK FIRST confirmed (train/gradnorm_a_mean and train/gradnorm_b_mean both present, 30/30 rollouts, nonzero, dual-core branch of _gradnorm_diag_ctx fired). Read (paired by call-order, not wandb _step, per anchor8's own pitfall): core B (stance) raw pre-clip grad L2 norm is >=3x core A (walk) in 23/30 logged rollouts, median b/a=4.2x overall and 3.5x isolated to the reward-trough window (rollout idx 6-13, this run's own reward quarters [38.5,15.5,-275.6,-24.1]), max 16x -- never comparable let alone inverted on THIS seed. Per this arm's own pre-registered decision rule that is SUPPORTED (not "REFUTED, every shared-computation mechanism now dead" -- this is the first of the four candidates -- log_std/trunk/advantage-norm/grad-clip -- whose own-seed read actually landed in the predicted direction). Built+tested (8 new unit tests incl. a synthetic-grad behavioral proof the two cores actually decouple) and launched the prescribed follow-up, train.bc_anchor_percore_clip (default 0, bit-exact off): replaces sb3-contrib's ONE global clip_grad_norm_ call with three independent per-group clips at the same max_norm, factored through a new shared _dual_core_param_groups helper (also used to refactor _gradnorm_diag_ctx, byte-identical grouping, zero behavior change -- gradnorm tests still green). Launched cw-standwalk-stance-mesh2-stage2-dualbc1-anchor10-percoreclip{,-s1} on the identical anchor9-gradnorm recipe with only the mechanism flag added. IMPORTANT CAVEAT (concurrent cycle's own seed1 read, f580f822): anchor9-gradnorm-s1's own gradnorm trace does NOT replicate this seed0 support -- seed1 full-run median b/a=1.95x (only 10/30 rollouts clear >=3x) and at the identical trough window the ratio is comparable-to-INVERTED (median 0.73, core A often the larger raw gradient there) -- so the JOINT call across both seeds is DIVERGENCE, not a clean cross-seed SUPPORT, joining every other architecture-sharing candidate (log_std, trunk, advantage-norm) in having at least one seed disagree. anchor10-percoreclip is still the right next step (it is a direct behavioral test of walk-survival, not contingent on the diagnostic being cross-seed-clean) but neither its pass nor its fail should be read as dispositive proof/disproof of grad-clip-coupling specifically -- either outcome routes to the reward/task-level audit if walk still fails. Verdict on THIS run (seed0): CANARY FAIL - MECHANISM (own-seed SUPPORTED, joint DIVERGENT with -s1's already-recorded read). Evidence: logs/experiments/cw-standwalk-stance-mesh2-stage2-dualbc1-anchor9-gradnorm/wandb_history.csv (train/gradnorm_{a,b}_mean, 30 paired samples); rl_move/sim/bc_anchor.py _gradnorm_diag_ctx/_percore_clip_ctx.

