# cw-standwalk-stance-mesh2-stage2-dualbc1-anchor10-percoreclip

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: RUNNING

**created**: 2026-08-27T13:24:49+00:00

**pod**: hexapod-mjx-train-4

**steps**: 2000000

**parent**: cw-standwalk-stance-mesh2-stage2-dualbc1-anchor9-gradnorm

**wandb_id**: 9ioyv0v6

**hypothesis**: Plain sentence: check whether decoupling PPO's single global gradient-norm clip into one clip per dual-core branch protects walk from stance's much-larger raw gradient. anchor9-gradnorm MEASURED (2M-step canary, seed0): core B's (stance) raw pre-clip grad L2 norm is >=3x core A's (walk) in 23/30 logged rollouts, median ratio 4.2x overall and 3.5x even isolated to the reward-trough window (idx 6-13), max 16x -- persistently, not just during the trough, never comparable let alone inverted -- SUPPORTING the shared-clip-coupling hypothesis per this arm's own pre-registered decision rule (every other proposed shared-computation coupling -- log_std anchor4/5/6/6b, trunk-gradient anchor7, advantage-normalization anchor8 -- has now been tested and refuted; this is the last one on that list). Built + unit-tested (8 new tests incl. a synthetic-grad behavioral test proving the two cores are actually decoupled) train.bc_anchor_percore_clip (default 0, bit-exact off): replaces sb3-contrib's ONE clip_grad_norm_(self.policy.parameters(), max_grad_norm) call inside RecurrentPPO.train() with THREE independent clips (core a / core b / shared, same frozen grouping the gradnorm diagnostic already used, now factored into a shared _dual_core_param_groups helper) at the SAME max_norm -- so stance's dominant raw norm can no longer force a single rescale factor onto walk's already-small, otherwise fully independent gradient. Launched on the IDENTICAL anchor9-gradnorm recipe (same seed, same init-from anchor2/anchor2_s1, same goal-mix/DR/mode_seq) with ONLY the new mechanism flag added (bc_anchor_debug_gradnorm left on too, harmless/read-only, for a same-run before/after cross-check since it measures PRE-clip norms regardless of which clip implementation is installed). Prediction-if-true: walk survives (det gait_valid climbs off the anchor4-class 0/6 freeze floor, ideally toward fix's clean 6/6) while hold/lower keep whatever protection the log_std anneal was already giving them (unaffected -- the anneal touches log_std_b, not the clip). Prediction-if-false: walk still shows the same total leg-sacrifice freeze even with the clip genuinely decoupled (checkpoint-verified via WIRING CHECK FIRST) -- rules out every proposed architecture-sharing/optimizer-coupling mechanism in this design, and the next candidate becomes reward/task-level (is the walk objective itself simply harder to optimize under this exact recipe's goal-mix/DR schedule, independent of any cross-mode interference) per anchor9-gradnorm's own gate text.

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY: do not judge skill acquisition or require a mature gait at 2M steps -- judge only whether decoupling the clip rescues walk from the anchor4-class catastrophe (or fails to). WIRING CHECK FIRST: before reading gait_valid, confirm via a direct checkpoint/code check that bc_anchor_percore_clip actually fired on the dual-core branch this run (cfg_set has train.bc_anchor_percore_clip=1 AND the policy has mlp_extractor_b) -- a wiring miss here (e.g. a plain load path that fails to attach the dual policy_kwargs) has bitten this exact campaign before (anchor6-logstdsplit CANARY FAIL - INFRASTRUCTURE). Read (WALK-SURVIVES): det gait_valid_frac at DR-0 -- >=5/6 with no persistent 2+-leg sacrifice = mechanism WORKS, promote per-core clip as the standing default for this recipe family; 1-4/6 with a milder/partial drag (matching anchor7-detachtrunk's rescued-seed1 shape) = PARTIAL, informative, dose/placement follow-up; 0/6 with the SAME total leg-sacrifice-freeze signature as anchor4/5/6/6b/7/8/9's own baseline = REFUTED -- every dual-core architecture/optimizer-sharing coupling mechanism has now been tested and refuted; escalate to a reward/task-level audit of the walk objective itself (is walk simply the harder-to-optimize half of this exact goal-mix/DR recipe, independent of any cross-mode coupling), not another architecture-sharing arm. Joint call across both seeds per the campaign's standard practice (seed-level noise vs a real cross-seed effect) -- do not verdict off one seed alone unless the other seed's own eval is still mid-computation and the wiring/read is otherwise unambiguous.

