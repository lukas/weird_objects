# cw-standwalk-stance-mesh2-stancemix-tuckclock-stdreopen-s2

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FINISHED

**created**: 2026-08-26T02:22:57+00:00

**pod**: hexapod-mjx-train-4

**steps**: 2000000

**parent**: cw-standwalk-stance-mesh2-stancemix-tuckclock-stdreopen

**wandb_id**: s9yxuq93

**hypothesis**: Is the stdreopen-acq8m pair's sharp seed divergence (seed0: 2/12 flat valid_plant FAIL, seed1: 11/12 PASS, same exact recipe/budget/warm-start) real seed-sensitivity in this recipe, or was seed0 simply unlucky? This is a 3rd-seed replication of the 2M stdreopen canary itself (identical recipe: warm from stancemix_bcchain3_stdanneal, mesh ref + flat-time-indexed BC-anchor clock, log-std 0->-4 anneal-frac 0.5, goal-mix hold=.1/rise=.45/lower=.45), seed=2, ONLY the seed changed. Prediction-if-reliable: seed2's 2M flat probe lands in the isolated tuckclock-acq8m precedent's own family of outcomes (partial-but-improving, matching seed1's eventual trajectory) rather than seed0's total-freeze/no-movement signature -- supports promoting seed1's checkpoint (or funding an 8M seed2 continuation) as the reliable recipe. Prediction-if-unreliable: seed2 also freezes/pins hard at 2M like seed0 -- supports the campaign's own precedent (isolated tuckclock arm replicated 24/24 cross-seed) that THIS particular seed's basin is bad, i.e. real per-seed variance requiring either more seeds or an initialization fix (e.g. warm-start from the solved riseonly ckpt, the parallel seqrise arm already testing that lever).

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. CANARY (2M, single seed, 3rd datapoint for the stdreopen recipe's cross-seed reliability question): reads alongside seed0 (2/12, FAIL) and seed1 (11/12, PASS) already posted. If seed2 pattern-matches seed1 (partial-to-good tuck, clear improvement vs a fresh-seed 2M baseline, no majority OC-pin) -> recipe is seed-robust-enough (2/3), seed0 was the unlucky outlier -> promote seed1 ckpt / fund seed0-reseed's own 8M; if seed2 pattern-matches seed0 (total freeze, 0 movement) -> recipe is genuinely ~1/3 reliable -> DO NOT promote on a single seed, fund either a broader seed sweep or adopt the seqrise (solved-checkpoint warm-start) lever as the fix instead of this recipe as-is.

**verdict**: CANARY FAIL - MECHANISM. Seed-2 of the stdreopen recipe matches seed-0's total-freeze pattern, NOT seed-1's clean pass -- and a concurrent cycle's dig-in on the sibling seqrise run just found the mechanical reason all three outcomes differ. Evidence: flat-pinned probe (goal.rise_flat_frac=1.0/partial=0/rsi=0, det+sto 6+6) is 0/12 valid_plant -- WORSE than seed0's 2/12 -- every episode over_current-terminated at the exact 2.64A ceiling, herr_end 9.7-61.9mm never settling, swing_count essentially zero on every leg/episode (0 total swings det, 2 total sto), video-confirmed identical splayed press-up to seed0/tuckclock1 (contact sheet: legs spread flat, no tuck, no belly lift). Standard mixed-start DR-0 gate stays healthy regardless: hold 6/6+6/6 zero-term, lower 6/6+6/6 honest (herr<=3.1mm, success=true), rise (mixed bridge/rsi/flat draws) 5/6 det+2/6 sto = 7/12 -- consistent with every other seed in this lineage, confirming the flat start specifically is the broken sub-case. Own-DR(0.2) similar shape (hold 6/6+6/6, lower 5/6+6/6, rise 3/6+3/6). Why: a concurrent cycle (seqrise dig-in, encoded in CURRENT_TRUTHS 'STANDWALK CANONICAL STANCE RECIPE' correction) discovered --log-std-init is a SILENT NO-OP under --init-from -- so this ENTIRE warm-started sub-lineage (stdreopen/-s1/-acq8m/-acq8m-s1/seqrise/-s1/this run) trained at the PARENT's pinned std 0.0183 from step 0, never the intended re-opened exploration. The 'std reopen' lever this run's own hypothesis tested never existed; what actually varies across seeds is optimization-path fragility of pinned-std warm-started mix training on the flat-rise basin -- 3 seeds now read s0=2/12, s1=11/12, s2=0/12, i.e. genuinely ~1/3 reliable, matching this canary's own pre-registered 'seed2 matches seed0' FAIL branch. Next: per the pre-registered branch (broad seed sweep OR adopt a different lever, not promote on 1/3 seeds) -- superseded by the concurrent cycle's own better-targeted fix: a FROM-SCRATCH 3-way-mix arm with a REAL std anneal (matching the only recipe that solved flat rise reliably, riseonly-tuckclock-acq8m 24/24) is the right lever, not more stdreopen-recipe seeds (that recipe's pinned-std mechanism is now understood and not worth re-running). That fix (scratch8m, seed 0) was launched this cycle by the concurrent cycle but its first attempt was mechanically REFUSED (pod code-sync) and only the retried lone seed ended up running; I completed the pre-registered pair -- scratch8m VERIFIED RUNNING train-6 (their relaunch), scratch8m-s1 VERIFIED RUNNING train-7 (this cycle, exact same recipe, seed=1, per the 08-22 batching rule). No further stdreopen-recipe seeds warranted.

