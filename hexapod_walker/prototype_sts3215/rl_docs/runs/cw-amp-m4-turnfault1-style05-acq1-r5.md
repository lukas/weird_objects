# cw-amp-m4-turnfault1-style05-acq1-r5

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PASS-partial

**created**: 2026-08-23T02:07:03+00:00

**pod**: hexapod-mjx-train-4

**steps**: 6000000

**parent**: cw-amp-m4-turnfault1-style05

**wandb_id**: i0u4b9gm

**hypothesis**: Plain English: turnfault1's 2M discovery stacked three brand-new axes at once (full-heading translation + turn-in-place + faults) and missed its own gait_valid>=10/12 safety bar (9/12, 3 sto leg-statues) while training reward was STILL RISING (46->203/quarter, never flat) -- per the 08-21 ruling that reads UNDERTRAINED, not a proven-broken combination, since push (pushacq1) and fault (faultobs2-headingsfull) each needed the SAME 3x acquisition step-up past their own 2M discovery reads before they composed cleanly. Single lever vs turnfault1-style05: continue from its own 2M checkpoint (--init-from-source) for 6M more steps, identical recipe (heading+turn+fault cfg, amp 0.5/0.5) unchanged, matching pushacq1's own acquisition dose exactly. (r5: r4 LAUNCH_CRASHED on a stale --obs-pad-transplant 18 inherited unchanged from the parent's graft-onto-new-substrate launch -- this respec's own source checkpoint already has the fault pad baked into its 93-dim obs, so continuing it needs pad=0, exactly like pushacq1's own continuation never re-pads; fixed via --arg, everything else identical, r1-r4 were tag/marker/config-crash retries, zero steps trained on any of them.)

**gate**: Acquisition (6M total, DR-0, own cfg). Mechanism-safety bar unchanged from the discovery arm: gait_valid >=10/12 det+sto (was 9/12), faulted episodes limp not statue on video, no crouch. If it clears: compare faulted-episode prog_ratio/slip against faultobs2-headingsfull-style05's own numbers (det 1.09/3.08, sto 0.57/7.26) and eval_yaw turn tracking against turnclone-yawcmd0-r2's own numbers (tip errs ~0.15-0.19) to see whether the 3-way combination now matches solo-axis quality at matched budget. If it STILL misses the safety bar with reward now flat: the combination is genuinely harder than either pairwise composition and needs the sequential (turn+push solid first, then graft fault) route instead of more raw budget on a fresh 3-way stack.

**verdict**: Plain English: more training time fixed the leg-statue safety problem this 3-way stack (turn+heading+fault) had at 2M, but it did NOT teach the robot to turn in place -- that failure was already baked in before this run started and 6M more steps left it completely unchanged. Evidence: DR-0 own-cfg gate (fault_prob=1.0 baked into the eval cfg) gait_valid 12/12 det+sto (was 9/12 at 2M discovery with 3 sto leg-statues), zero terminations, zero sacrificed legs anywhere -- the run's own named mechanism-safety bar (>=10/12) clears cleanly and decisively. Progress/slip land in the same ballpark as the solo-axis fault comparison (faultobs2-headingsfull-style05 det 1.09/3.08, sto 0.57/7.26 prog/slip): this run's det med ~0.90/4.4, sto ~0.62/6.3. BUT the run's own second named comparison (eval_yaw turn tracking vs turnclone-yawcmd0-r2's 0.15-0.19) fails hard: hand-run eval_yaw panel (matched 93-dim cfg incl. fault_prob=1.0) gives tip-left/right err 0.2985/0.3002 rad/s -- essentially equal to |wz_ref| itself, i.e. the policy does not turn in place AT ALL -- vs turnpush1-style05-acq1-r2's 0.14/0.12 and turnclone-yawcmd0-r2's 0.15-0.19. Root cause isolated by running the IDENTICAL panel on the 2M discovery checkpoint: it ALSO reads tip err 0.30/0.30 (arc tracking a hair better at 2M: 0.13-0.17 vs 0.16-0.23 here) -- the tip-park defect predates this run entirely; the acquisition budget fixed gait safety but never touched (and did not worsen) turn-in-place tracking. Same defect class as the M2 turn-income finding (08-23 ~01:40 audit): hold/forward reward income dominates the tip-in-place cell, so a FRESH 3-way stack's optimizer never bothers to learn tip response, unlike the pairwise turn+push substrate (built from the turn-taught BC clone) which had already partially learned it before push/fault were grafted on. Video (all 12 det+sto strips) confirms clean six-leg cycling throughout, no flag-leg/dragging/statue -- the walking itself is genuinely good, it simply never rotates on the spot. Verdict: PASS on the run's own primary named gate (mechanism safety); the secondary named comparison (turn quality vs solo-axis) is an open gap, not a fresh regression caused by this run. Next: the sequential composition route already relaunched this cycle (cw-amp-m4-turnpushfault1-style05-r2, fault grafted onto the turn+push-solid checkpoint instead of a fresh 3-way stack) inherits the turn+push substrate's actual tip response -- that comparison will show whether sequencing (not just budget) is what recovers turn-in-place under fault composition.

