# cw-amp-m3-turnpush1-style05-acq1-r2

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: INFORMATIVE-turn-eroded

**created**: 2026-08-23T01:59:52+00:00

**pod**: hexapod-mjx-train-1

**steps**: 6000000

**parent**: cw-amp-m3-turnpush1-style05-acq1

**wandb_id**: wlu57rof

**hypothesis**: Plain English: turning while being shoved looked bad at the 2M quick-look, but the robot was still learning fast when we stopped — give it the same full budget the non-turning push walker needed. Acquisition continuation of turnpush1-style05-r2 (turn-capable substrate + 10-25N single shove), 6M more from its own ckpt, dose unchanged — the direct test of the 'just needed more steps' read vs the sequential-composition alternative named in STATUS. Prediction-if-true: det prog med recovers toward the pushsmoke1-style05 band (>=0.9 vs r2's 0.37), topples <=1/6 det + <=2/6 sto, and eval_yaw tip err stays within ~0.05 of the substrate's 0.15-0.16 (turn not sacrificed for push survival). Prediction-if-false: reward flattens while prog stays <0.6 or turn tracking collapses to the park fingerprint (0.28-0.33) — budget refuted for the joint skill; the named next is SEQUENTIAL composition (solidify turn+push force staging separately) per the M2->M3->M4 milestone ordering. Strongest alternative: prog recovers but turn erodes (the yawcmd0 income-audit erosion mechanism re-fires under push) — then the yaw_prog_overshoot_decay/yaw_prog_avg_s repricing keys are the follow-up, pending the yppeak arm's readout. (Infra retry: verbatim relaunch of a REFUSED stale-pod-code-marker stub via respec --now sync path; no spec change.)

**gate**: Acquisition (6M continuation, 8M total, DR-0). PASS = own-cfg gate (push on, yaw cfg on) det prog med >=0.9, topples <=1/6 det + <=2/6 sto, gait_valid >=5/6 det+sto, zero sacrificed, AND eval_yaw tip errs <=0.21 (within 0.05 of substrate 0.15-0.16); video shows turning under shove absorption. INFORMATIVE-budget-refuted = reward flat with prog <0.6 => sequential composition is the named lever. INFORMATIVE-turn-eroded = prog recovers but tip errs >0.21 / park fingerprint => turn-vs-push interference, repricing keys next. FAIL = collapse/statue/NaN.

**verdict**: SELF-CORRECTION (same cycle): the earlier PASS verdict on this run's eval_yaw branch was WRONG — my manual eval_yaw check omitted the training servo profile (bus.write_speed/write_acc/servo_vel_max_counts_s/safety.max_delta_q_deg), silently defaulting to the gentle bus profile (write_speed=400/write_acc=20) instead of the trained fast profile (1500/80) — the exact self-inflicted eval-cfg bug already flagged once before on the joystick track (stotight45 second-seed-base re-eval note). Re-ran through the just-built eval_amp_m5 harness (which always sets the correct bus cfg) AND independently by hand with the correct bus cfg: tip-left/right err = 0.3838/0.4302 (my wrong reading: 0.1431/0.1152) -- both FAR over the 0.21 bar and WORSE than the historical park/statue fingerprint (0.28-0.33 rad/s). The substrate baseline (yawcmd0-r2, 0.1525/0.1614) reproduces EXACTLY through the same M5 harness, confirming the baseline was fine and only my manual check was misconfigured -- this is not a substrate-vs-substrate measurement error, it is a real, large turn-vs-push interference. CORRECTED VERDICT: INFORMATIVE-turn-eroded, this run's own pre-registered branch ('prog recovers but tip errs >0.21 / park fingerprint => turn-vs-push interference, repricing keys next'). The walk-side numbers in the withdrawn PASS stand unchanged (DR-0 own-cfg gate det prog med 1.113, topples 0/6 det+2/6 sto, gait_valid 6/6, zero sacrificed -- all still correct, only eval_yaw was misconfigured) but turn tracking IS badly sacrificed for push survival, not preserved. NEXT: apply the already-built+banked yaw_prog_overshoot_decay/yaw_prog_avg_s repricing keys (built for the yppeak arm) to this substrate instead of treating it as acquisition-solid. CAVEAT: this cycle already launched cw-amp-m4-turnpushfault1-style05(-r2) on the (now-refuted) premise that turn+push was solid -- letting the cheap 2M discovery finish since it is already running, but its 'graft fault onto a solid substrate' framing is invalid; re-read its result knowing turn was already broken on this substrate before fault was added, and do not cite it as sequential-composition evidence without re-verifying turn tracking with the CORRECT bus cfg.

