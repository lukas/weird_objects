# cw-dep-bcgait4-phasedir8-emakernel-allow24

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: RUNNING

**created**: 2026-08-22T09:45:01+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-dep-bcgait4-phasedir7-dragstance

**wandb_id**: t3a0kl9t

**hypothesis**: The robot slowed to just under the gate's speed floor in phasedir7/7b because the reward taxed ALL travel, not cheating — this arm repairs the two measured pricing defects so the honest gait is the reward's actual optimum for the first time. Dig-in measurements on the ACTUAL checkpoints (probe_stance_slip_dist, det DR-0, matched env): (A) the 6mm per-stance drag allowance sits BELOW the honest tibia-150 gait's own slip tail (clone pooled median 3.4mm but p90 12.3/p95 19mm; the honest clone paid 2.87x its ENTIRE income in drag-stance charge at allow=6/k=8000) so travel itself was net-negative at ANY k — that is the step function: k=8000 and k=4000 share the same gradient direction (every tail stance pays regardless) and the same boundary optimum; (B) the tracking kernel's INSTANTANEOUS 2D velocity error taxes honest stride sway so hard that income was FLAT in realized speed across [0.059,0.08] m/s — the 0.059 m/s pd7 gait out-earned the 0.0716 m/s clone on the kernel itself (357.1 vs 325.6/ep), the sway tax (-31/ep) cancelling k_walk_prog's speed payment (+34/ep). Changes vs phasedir7 (k_drag_stance=8000 kept): drag_stance_allow_mm 6->24 (clone ~p98: honest pays 0.10x income on ~2% of stances; the pd6 drag gait still pays 0.49x — 08-11 audit intent restored at the measured plant), NEW reward.walk_kernel_vel_ema=1 + walk_kernel_vel_tau_s=0.75 (kernel error on a stride-EMA of body velocity, the k_walk_course convention, so zero-mean sway averages out; new flag default-off, bit-exact off, verified), k_walk_prog 1->2 (restores a clearly positive income slope in speed; overspeed still priced by the along-EMA band + prog cap 1.25). Full-stack checkpoint pricing now orders clone 1031 > pd7-slow 978 > pd6-drag 639 (at allow=20 without B it was clone 684 < slow 806 — B is necessary, not decoration; at allow=6 clone was -1536). Bank: test_phasedir_semantics 29/29 incl new rows (EMA-kernel inertness bit-exact, obey>shrunk det AND at the std-0.13 training regime, anti-attractor orderings survive the reprice, honest drag charge <35% of walk income, income slope positive). Prediction-if-true: det speed recovers >=0.06 with progress >=0.9x clone and slip <=1.15x clone (kernel/prog now pay for the clone's own gait; drag charge ~0 on it). Prediction-if-false-(i): speed/progress recover but slip misses the 1.15x cap — 24mm spares too much of the drag margin PPO explores; next is allow 16-20 with the B fixes kept. Prediction-if-false-(ii): still pinned ~0.059 with drag charge ~0 and reward rising — pricing is NOT the binding constraint; the BC-anchor/phase-lock family boundary is; dig into anchor dose/phase_hz, not reward. Prediction-if-false-(iii): overspeed (speed>0.096 or the 0.139 attractor) — k_prog=2 overpowered the course band; retry k_walk_prog=1.5, single change.

**gate**: At 2M, DR-0, forward panel (same eval_checkpoint det+sto invocation and cfg-sets as phasedir7's gate, PLUS the four new cfg keys) on the final checkpoint, clone-relative against the SAME frozen control (logs/ckpt_eval/phasedir3_clone_control_gate — do not re-run it). PASS requires ALL: (a) zero falls, gait_valid 6/6; (b) progress >= 0.9x clone; (c) slip/m <= 1.15x clone; (d) dir_err med <= clone + 5deg; (e) speed_mean in [0.06,0.096]. VERDICT MUST additionally report: policy_std (expect ~0.13), W&B env/reward_drag_stance trajectory (expect near-zero from step 0 — if it grows large+negative the policy found a NEW drag regime beyond 24mm/stance: name it and re-probe the distribution), walk_kernel_vel_ema_err (new info key, should track ~|v_ema - 0.08|), and per-leg swing count/stride/duty vs clone AND vs pd7's slow signature (swing ~20/leg) — PASS should move swings/leg back toward the clone's ~30 at speed >=0.06. Per the 08-21 ruling: if gate misses but reward AND gate metrics (speed, slip ratio) are still improving at 2M, continue from checkpoint before re-specing. PASS -> pre-registered rung B respec: goal.walk_heading_set=[0,0.7854,-0.7854], same gate per heading bin. FAIL branches: see hypothesis predictions (i)-(iii), each names its single next change. NO DOWNLOAD_ANSWER change from this run.

