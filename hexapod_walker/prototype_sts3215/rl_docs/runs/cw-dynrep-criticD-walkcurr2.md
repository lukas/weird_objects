# cw-dynrep-criticD-walkcurr2

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: SUPERSEDED_NONCOMPLIANT

**created**: 2026-08-18T06:57:20+00:00

**pod**: hexapod-mjx-train-7

**steps**: 40000000

**git_sha**: 5aa3feaa4af993f0d2bf4931c72e40d706e278dc

**wandb_id**: 05ex0rqz

**hardware_ready**: False

**hypothesis**: Fix the two things that made the first attempt at an adaptive walking curriculum (cw-dynrep-criticD-walkcurr1) worse than just training on a fixed random mix of commands, then relaunch with a fresh actor. This arm tests whether a corrected ignition band (real speed + heading variety from the very first bucket, so standing still doesn't already score well) plus a corrected certification bar (matching what a genuinely good walker's numbers actually look like, not an arbitrary bar the best known policy would itself fail) plus a healthier PPO update path (capped epochs, a target KL, decaying actor learning rate, and automatic rollback of any update that moves the policy too far) lets the curriculum actually promote past its first rung and beat the fixed-mix twin (cw-dynrep-criticD-40m1) at matched or better slip/roll. Operator MCP note fb_20260818T060044 (GPT-5 Codex for Lukas): 'figure out how to make a great run and then launch it.'

**gate**: Pre-registered 40M decision checkpoint, no extension without a verdict. Judged on the BEST checkpoint = last retention-clean promotion (never reward/latest). PASS = (a) frontier reaches >= B6 (stop/restart mastered with retention) by 40M, AND (b) on the shared 6-episode command-rich own-DR walk eval the promotion checkpoint meets: early_term_rate 0, cmd_prog_frac >= 0.6, slip_per_m <= 2.0, peak_roll_deg <= 8, contact_sw_per_s >= 3 (all six feet cycling), AND (c) beats cw-dynrep-criticD-40m1's retained 6M-best checkpoint (NOT its degraded 40M final) on slip/roll at matched or better command progress -- a higher-reward pick with worse drag/roll is not a PASS. Mechanical invariants: encoder md5 match, pred/snapshot_version 0, per-bucket walkcurr/* W&B panels advancing, promotions saved, no locked-bucket training (cert logs), train/actor_lr decaying, train/kl_rollback_count logged. Verdict must quote per-bucket cert table + visual-quality stats vs both the 6M-best AND the degraded-40M-final numbers of the parent.

**verdict**: SUPERSEDED_NONCOMPLIANT (operator order fb_20260818T065930_03b422) — NOT a behavioral FAIL. The run trained C-MuJoCo physics on CPU via SubprocVecEnv (n_envs 16, only Torch on the H200), which the operator ruled noncompliant with the all-GPU requirement; stopped at ~9.0M/40M steps once the compliant replacement cw-dynrep-criticD-walkcurr3 (train_ppo_mjx, MjxShardedVecEnv impl=warp n_envs 4096, backend fail-closed-verified, W&B 4dih5ztm) was VERIFIED RUNNING. State at stop: frontier still B0 (no promotions), last cert b0_ignition cmd_prog_frac 0.11, contact_sw 1.8, falls 0 — 9M-step scratch-actor state, judged nothing. Checkpoints ck4M/ck6M/ck8M preserved on hexapod-mjx-train-7:rl_move/dynamics/models/. Curriculum/update-health design carries forward unchanged into walkcurr3.

**note**: Script-owned (pod_criticDwalkcurr2.sh, manifest criticDwalkcurr2_manifest.jsonl on-pod). Operator MCP note fb_20260818T060044 (GPT-5 Codex for Lukas, 'figure out how to make a great run and then launch it'). Code 5aa3feaa (main, tag exp/cw-dynrep-criticD-walkcurr2 + exp/cw-dynrep-criticD-walkcurr2-runner). Fresh scratch actor seed 8, exact frozen vt2ovznc critic D (md5 9df48f687967c25085ee50171e4110ff asserted), 40M steps, n_envs 16, CUDA. THREE coupled changes vs cw-dynrep-criticD-walkcurr1 (repealed one-variable-per-run; the cycle judged the coupling necessary -- a curriculum redesign is not fairly testable on an update path independently shown to be unstable): (1) goal.walk_curriculum=2 selects WALKCURR_BUCKETS_V2 in walk_task.py -- B0 ignition speed 0.08-0.12 m/s + heading spread from the first bucket (was 0.04-0.05 m/s dead-ahead, inside SIGMA_V=0.05's velocity-tracking kernel width, where parking scored ~67% of peak) and cert-gate slew_sat_max relaxed 0.5->0.95 (the known-good cw-dynrep-criticD-40m1 6M-best checkpoint runs slew_sat~0.925 and would fail V1's hard bar outright), with a per-bucket ignition/quality gate tier; (2) update-path health ported from rl_move/sim/update_health.py (already proven in train_ppo_mjx.py): n_epochs 10->3, target_kl 0.01, actor(1e-4->1e-5 linear decay)/critic(3e-4 constant) optimizer groups (CRITIC_MARKERS extended with value_gate/latent_adapter for this policy -- test_dynrep_predictive_critic.py pins the split), transactional KL-rollback at realized approx_kl>0.03 -- fixes the exact failure signature cw-dynrep-criticD-40m1's own training log showed (loco_quality peaked 6M, collapsed by 35-40M as approx_kl/clip_frac climbed unchecked, no target_kl/rollback/LR-decay in this trainer ever before); (3) unchanged from walkcurr1: all-env admission broadcast, frontier+retention promotion (cert every 0.5M, n=8/bucket), 2-consecutive-retained-failure rollback, best=last retention-clean promotion. All new code default-off/bit-exact-when-off; CUDA mechanism canary (canary-walkcurr2, 300k steps, exact CLI) verified clean before this launch: update-health split correct (7 actor/11 critic tensors), 3 cert rounds ran on the V2 table with no crash, walk return improved -20->192 in the short window.

