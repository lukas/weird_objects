# cw-amp-m4-turnfault-seq1-pushcont1-tipfrac05

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PASS

**created**: 2026-08-23T05:50:17+00:00

**pod**: hexapod-mjx-train-2

**steps**: 2000000

**parent**: cw-amp-m4-turnfault-seq1-pushcont1-ypfix1

**wandb_id**: 30yc8ym1

**hypothesis**: Plain English: does giving the policy real turn-in-place PRACTICE, now that it actually knows how to rotate (the BC turn-clone init already gave it a working omega motor pattern), let it learn ACCURATE tip tracking instead of settling for richer hold/forward income? This exact curriculum lever (goal.walk_turn_in_place_frac -- whole-episode dedicated turns, 50/50 sign) was tried and REFUTED once before (tip50/tip90, both parked at |wz_ref| exactly, zero dose-response 0.5->0.9) -- but that was BEFORE the BC turn-clone fix landed, when RL had NO discoverable turning motor pattern at all, so densifying exposure to an undiscoverable skill did nothing. It has never been retried post-clone. Fresh evidence this cycle (probe_walk_income on pushcont1-ypfix1, hazards zeroed): a pure heading-hold segment nets 1415 total return vs 842/796 for perfect tip_left/tip_right turning -- not because hold is over-PRICED (a true zero command SHOULD pay max) but because turning genuinely costs 4-10x more in current/gyro/roll charges than standing still, and turn-in-place segments are only ~7.5% of independently-sampled training exposure -- too rare for the optimizer to prioritize nailing a skill that costs more than it can ever out-earn hold/forward on a per-segment basis. Densifying WHOLE-EPISODE turn exposure raises turning's share of total training-time reward without touching any per-tick price (lower exploit risk than another pricing key). Single lever vs pushcont1-ypfix1 (this cycle's PASS-partial verdict, tips 0.2471/0.2553): add goal.walk_turn_in_place_frac at a dose grid, matched everything else (same overshoot-decay/avg_s pricing keys ON, same permanent fault/push cfg, same 2M discovery budget, same pre-cheat turnfault-seq1 init per the init-basin rule).

**gate**: PASS-clean = tip-left AND tip-right eval_yaw err <=0.20-0.25 (closes the M4/M5 turn+push cell outright). IMPROVED = measurably better than ypfix1's own 0.2471/0.2553 but still misses. FLAT/WORSE = no improvement or regression -- extends the pre-clone tip50/tip90 refutation to the post-clone/composed regime, meaning command-exposure is refuted twice now and the residual lever really is a per-tick repricing (charge hold/forward more, or a milestone-style tip-completion bonus) rather than curriculum. Hold-and-report regardless: own-cfg DR-0 gait_valid must stay >=9/12 (safety must not regress for more turn exposure), and eval_amp_m5 push/fault sections must stay PASS. KNOWN GOTCHA: dedicated whole-episode turn commands read as near-zero-progress on the standard DR-0 harness by design (contamination, not collapse) -- judge translation quality from the non-tip episodes and read tip accuracy from hand-run eval_yaw, not the raw panel median.

**verdict**: FIRST-EVER FULL M5 PASS: densifying turn-in-place PRACTICE (not pricing) closes the M4 turn+push+fault composition cell outright. Single lever vs pushcont1-ypfix1 (this cycle's PASS-partial, tips 0.2471/0.2553): goal.walk_turn_in_place_frac=0.5 (whole-episode dedicated turns, 50/50 sign), same 2M budget, same overshoot-decay pricing keys, same permanent fault/push cfg, same pre-cheat turnfault-seq1 init. Result: eval_amp_m5 (the AMP track's own M5 cross-engine DONE-gate suite) reads m5_pass=TRUE -- all four sections clear their bars: walk (0 terms, gait_valid 12/12, det prog 1.10, det slip 3.36 -- translation-only episodes, turn-in-place ones correctly excluded per this cycle's eval_amp_m5 fix), yaw (tip-left/right err 0.162/0.184, UNDER the suite's own strict <=0.20 bar on BOTH sides, and better than even the fault-only solo parent's 0.18/0.17), push (2/6 det + 0/6 sto terms <=bar, gait_valid 11/12, one legit carried-fault-leg), fault (same numbers, same bars, PASS). Video-reviewed (push section, the risk mode): det_1 and sto_4 clean six-leg cycling with a legitimate single carried fault-leg, zero pathology; det_3 shows honest walking for ~2.5s then a genuine tilt_roll knockdown on the LAST frame (a real late push fall, not a statue) -- matches the established push-recovery fingerprint from every prior M3/M4 arm, not a new defect. HYPOTHESIS CONFIRMED: probe_walk_income this cycle showed a perfect heading-hold segment nets ~1.7x a perfect tip-turn segment's return (1415 vs 842/796) purely from lower actuation cost, not mispricing, while turn-in-place is only ~7.5% of independently-sampled training exposure -- densifying WHOLE-EPISODE turn practice (this exact lever, refuted once BEFORE the BC-turn-clone motor-pattern fix, never retried after) raised turning's share of total training-time reward without touching any per-tick price. Clean monotonic dose-response across the 3-arm grid launched this cycle (frac 0.2/0.3/0.5 -> tip err 0.207/0.234, 0.201/0.214, 0.162/0.184 -- see tipfrac02/tipfrac03 INFORMATIVE verdicts) makes this a real, reproducible-looking mechanism, not a lucky roll. CAVEAT (why this is not yet a track-DONE declaration): single seed (seed=7, inherited unchanged) -- a repro arm (same recipe, seed=13) is launched this cycle to check it is not a seed lottery before any promotion; push/fault sections are numerically identical (both hazards permanently baked in this checkpoint's own cfg, the known q_20260823T0130Z design tension) so they are not independently discriminating, only jointly; M6 hardware is explicitly operator-owned per tracks.json. Also fixed a real crash in eval_amp_m5.py this cycle (statistics.median choked on None progress_ratio for turn-in-place episodes; commits eval-amp-m5-none-median-fix + eval-amp-m5-turnplace-translating-filter) discovered by these exact arms -- every prior M5 checkpoint never had walk_turn_in_place_frac>0 baked in so the gap never fired before. Evidence: logs/ckpt_eval/cw_amp_m4_turnfault_seq1_pushcont1_tipfrac05_{gate,m5}/.

