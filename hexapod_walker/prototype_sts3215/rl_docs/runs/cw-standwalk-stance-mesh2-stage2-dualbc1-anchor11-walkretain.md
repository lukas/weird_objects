# cw-standwalk-stance-mesh2-stage2-dualbc1-anchor11-walkretain

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY FAIL - MECHANISM

**created**: 2026-08-27T14:10:20+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-standwalk-stance-mesh2-stage2-dualbc1-anchor6b-logstdsplit-fix

**wandb_id**: ny97wc4a

**hypothesis**: Plain English: make walking regression hurt DURING TRAINING instead of being discovered at eval (operator design correction 2026-08-27, MCP note 20260827T135505Z) -- turn the walk-tick BC anchor back ON so every drift of the walk core away from the proven teacher gait adds loss on every update. Every dualbc1 arm to date carried train.bc_anchor_walk=0.0, so walk had NO in-loss retention term -- the exact gap the operator names. Target dialect matches the walk teacher stotight45-seed13's OWN training recipe (bc_anchor_walk=1 + phase_lock=1 + knee_abs=1, the raw-module scripted TripodGait phase-locked to the policy's phase-obs clock; both the obs clock and the anchor clock reset per mode_seq segment switch -- walk_task._seq_reset_mode_state / sim_env switch path -- so they stay aligned in composed episodes). Recipe base = anchor6b-logstdsplit-fix: the hold-FIXING but walk-catastrophe-prone arm (per-core log_std split + stance-core anneal; seed0 clean walk + hold/sto 2/6, seed1 total walk catastrophe 0/6 gait_valid). Prediction-if-true: the restoring action-space gradient on walk ticks prevents the seed1-class collapse -- BOTH seeds keep walk alive while hold keeps the anneal gain. Prediction-if-false: a seed still collapses even with an explicit walk-retention loss -- the catastrophe is not action-drift-recoverable (e.g. value-fn corruption); route to the operator's second mechanism (per-mode-group objective normalization, unbuilt) rather than another architecture arm. Strongest alternative: walk survives but is pulled off the mesh-adapted gait toward the raw scripted tripod (prog/slip regress vs the anchor2 band) -- then the next knob is a per-mode anchor coefficient (global coef here is 3.0; the teacher's own walk anchor dose was 1.0).

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY at 2M, joint call with -s1; matched controls = anchor6b-logstdsplit-fix{,-s1} gate/owncfg evals. PASS if on BOTH seeds walk det DR-0 gait_valid>=5/6 with prog_ratio>=0.2 and zero sacrificed legs (vs fix-s1's 0/6 catastrophe) AND hold/sto DR-0 term <=2/6 (anneal gain kept, like fix seed0) -- that would be the first cross-seed full-recipe candidate; fund a 2x-seed acquisition wave next. PARTIAL-A (walk protected both seeds, hold/sto >2/6): retention works but costs the anneal gain -- fund per-mode anchor-coef split next. FAIL (any seed anchor4-class walk collapse: gait_valid 0-1/6, sacrificed legs, static video): walk-retention-by-scripted-anchor is refuted for this catastrophe class; per the operator note route to building per-mode-group objective normalization (default-off, bit-exact), NOT another log_std/trunk/clip arm. Read reward trend per the 08-21 ruling before any verdict.

**verdict**: CANARY FAIL - MECHANISM (seed0, own scope; JOINT CALL PENDING -s1, which is still training and belongs to no cycle yet): this seed0 was NOT the catastrophe case the rescue targets (its own untouched parent, anchor6b-logstdsplit-fix seed0, was already gait_valid 6/6 det+sto, prog_ratio 0.23/0.02) -- so this run cannot show a rescue, only a cost, and it shows the SAME cost anchor12's control showed on the other recipe family: DR-0 gate walk det prog_ratio 0.23->0.08 (-65%), slip/m 6.56->13.64 (+108%); sto prog 0.02->0.02 (flat, already near-zero), slip 29.42->31.64 (+8%, within noise); gait_valid held 6/6 det+sto (no new sacrificed-leg catastrophe), hold/lower stayed roughly in band (det lower improved 5/6->5/6, sto lower 3/6->3/6, det hold 4/6->4/6, sto hold 4/6->3/6). Per the gate's own text the PASS/PARTIAL-A/FAIL branches are joint-2-seed calls and its FAIL bar (gait_valid 0-1/6 + sacrificed legs) does not fire on this seed -- so seed0 alone is neither a PASS (prog_ratio 0.08 misses the >=0.2 bar) nor the gate's own FAIL trigger. Calling this CANARY FAIL - MECHANISM on the strength of the cross-run pattern (this is the SECOND recipe family, seed0, where turning the walk anchor on measurably taxes an already-non-catastrophic walk by a similar magnitude to anchor12's control) -- consistent with the anchor's pull itself being the cost driver, independent of which recipe it's applied to. The DECISIVE read (does it rescue seed1's real 0/6 collapse) is still -s1, unfinished; whoever triages -s1 should read this note first and issue the actual joint PASS/PARTIAL/FAIL call per the pre-registered branches, not re-derive them. Built+unit-tested (9 new tests) train.bc_anchor_walk_coef (default -1 unset = bit-exact; per-mode split when set) as the funded follow-up (-2.35b), triggered by anchor12's own-seed FAIL.

