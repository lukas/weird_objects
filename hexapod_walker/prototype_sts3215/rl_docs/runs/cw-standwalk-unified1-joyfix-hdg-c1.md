# cw-standwalk-unified1-joyfix-hdg-c1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY FAIL - MECHANISM

**created**: 2026-08-28T16:06:15+00:00

**pod**: hexapod-mjx-train-4

**steps**: 2000000

**parent**: cw-standwalk-unified1-mix-long-s0

**wandb_id**: lve8e2nw

**hypothesis**: Plain English: the unified walking policy barely responds to sideways joystick commands because training never draws them — the recipe pins every command heading to straight-ahead (walk_heading_max_rad=0.0), so lateral commands only ever appear via the square/sweep schedule rotations and diagonals never (08-28 audit: crab response ~7% of forward, frozen-state action deltas for diagonals 3x smaller than cardinals, plumbing verified healthy). This canary opens the command-heading draw to the full circle on the otherwise identical 60s mixed-session recipe, warm from long-s0's own 16M PASS checkpoint. Predict-if-true: at 2M the lateral (left/right) net-displacement speed ratio in the local response-matrix probe at least doubles from 0.033 while forward stays >=0.35 (parent 0.475) and session terminations do not explode. Predict-if-false: lateral stays ~0 or forward/session health collapses (would say lateral needs a staged heading-set curriculum, not full-circle at once). Strongest alternative: heading exposure alone is insufficient because the velocity-blind mode-2 obs can't support closed-loop lateral tracking (the velobs3/bundle arms decide this).

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. CANARY (mechanism health only, no behavior maturity claim): at 2M — training reward not collapsing (no sustained fall vs the long-s0 continuation trend at matched steps), DR-0 det walk gait_valid stays >=5/6 with no new sacrificed leg, session eval terminations not exploding (>2x parent's 0/90 baseline band = any >6/90 fails), AND probe_cmd_sensitivity response matrix on the 2M ckpt: max(left,right) speed_ratio >=0.07 (2x parent 0.033) with fwd >=0.35. PASS -> 2-seed acquisition; FAIL with healthy training -> staged heading-set curriculum arm instead.

**verdict**: CANARY FAIL - MECHANISM: opening the command-heading draw to the full circle (goal.walk_heading_max_rad=pi, single lever off long-s0's 16M PASS checkpoint) does NOT give the unified policy lateral tracking at 2M, and destabilizes training doing it. Evidence: probe_cmd_sensitivity.py (built+run this cycle, response mode, matched cfg stack) on the 2M ckpt: fwd speed_ratio 0.422 (healthy, >=0.35 bar) but max(left,right) speed_ratio = max(0.005, -0.006) = 0.006 — WORSE than the pin-forward parent's own baseline 0.033, far under the 0.07 (2x parent) target; net cross-track displacement for left/right commands (0.12-0.13m) is indistinguishable from the fwd/back drift itself, i.e. no real lateral response emerged. Training reward also failed its own health bar: rollout/ep_rew_mean fell from +68 (0.7M) to -310 (1.3M, a >350pt sustained crash spanning more than half the run) before partially recovering to -115 by 2M (still deeply negative, not clearly plateaued) — this is NOT 'no sustained fall vs the parent trend'. Both of the canary's own AND-gated criteria (reward stability, lateral response) are violated; this exactly matches the hypothesis's own predicted-if-false branch. Why: a single-jump full-circle command draw is too much curriculum width change for a 2M-step warm-start budget on this recipe — consistent with the joystick track's own prior staged-curriculum finding (never full +-180 from tick zero). What's next: per the canary's own pre-registered fallback, queued a staged heading-SET canary instead of a wider full-circle dose — cw-standwalk-unified1-joyfix-hdgset1 (same warm start/recipe, goal.walk_heading_set=[0,+45deg,-45deg], the joystick track's previously-used 'rung B' set), VERIFIED RUNNING this cycle. Extended probe_cmd_sensitivity.py with diag_fl/diag_fr response rows (RESPONSE_CMDS, exp/cw-standwalk-unified1-joyfix-hdgset1) so the next read tests the actual trained heading angles, not off-distribution pure-lateral. DR-0/own-DR/mixedsession harness evals for this run were still computing on-pod (severe CPU contention sharing hexapod-mjx-train-4 with a concurrent cycle's long-running eval, >1h45m and counting) at verdict time; they cannot flip this already-decided FAIL (2 of 4 AND-clauses already fail) and will land in logs/ckpt_eval/cw_standwalk_unified1_joyfix_hdg_c1_{gate,owncfg,mixedsession}/ for the record.

