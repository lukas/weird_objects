# cw-dep-bcgait4-phasedir6-slipband

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-22T07:33:59+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-dep-bcgait4-phasedir5-stdoverride

**wandb_id**: bjvgezls

**hardware_ready**: False

**hypothesis**: The phasedir5 policy walks obediently but finances its progress with dragged loaded feet because the loadslip charge band was so wide that slip was literally free; tightening the band to where the low-std rollouts actually live should make the same training run keep the obedience gains while pulling det slip back into the clone band. Identical to phasedir5 in every respect (same warm start, same --warm-log-std-override -2.0, same course/anchor/height stack, same 2M budget, seed 13) EXCEPT reward.loadslip_ok 7.0->3.0 and reward.loadslip_max 10.0->6.0 — sized from measured operating points (drag rollout ratio 4.2-4.7, clone-like std-0.13 rollout ~3.3-3.7, gate-passing det ~2.0-2.2 + ~1.3 noise floor) and validated by the pod pricing A/B in --evidence: the cheat gait now loses its rollout income (-30.5 vs +282.1) while the teacher-band det gait keeps 99.2%. Prediction-if-true: W&B env/walk_loadslip_ratio FALLS over training (from ~3.7-4.3 warm-start toward <=3.3) with walk_loadslip_factor meaningfully <1 early (band firing), and the det gate lands slip <=1.15x clone with progress still >=0.9x (phasedir5 proved 0.984x is reachable). Prediction-if-false-(i): ratio falls to the band but harness det slip STILL misses -> the env loadslip estimator diverges from the harness slip metric (noise-inflated numerator; next lever is a det-band slip estimator, e.g. stride-EMA or per-foot stance-slip pricing — NEW CODE, not another band value). Prediction-if-false-(ii): progress craters / idle-park behavior with income collapse from step 0 -> band too tight at the warm start; raise ok to 3.5 as phasedir6b (mechanism intact, dose wrong). Prediction-if-false-(iii): reward AND ratio flat all run -> the band lever itself is refuted for this lineage; stop repricing and go to the det-slip estimator.

**gate**: At 2M, DR-0, forward panel (same eval_checkpoint det+sto invocation and cfg-sets as phasedir5's gate) on the final checkpoint, clone-relative against the SAME frozen control (logs/ckpt_eval/phasedir3_clone_control_gate — do not re-run it). PASS requires ALL: (a) zero falls, gait_valid 6/6; (b) progress >= 0.9x clone; (c) slip/m <= 1.15x clone; (d) dir_err med <= clone + 5deg; (e) speed_mean in [0.06,0.096]. VERDICT MUST additionally report: policy_std from the eval report (expect ~0.13, override unchanged), the W&B env/walk_loadslip_ratio trajectory (must FALL toward <=3.3; if it never drops below ~4 the band did not bite) and walk_loadslip_factor (must sit <1 early). PASS -> pre-registered rung B respec: goal.walk_heading_set=[0,0.7854,-0.7854], same gate per heading bin. FAIL branch (i) ratio fell but det slip missed -> verdict names the env-estimator/harness divergence and the next arm is a det-band slip estimator (new code + bank rows), NOT a band value. FAIL branch (ii) progress collapse/idle -> phasedir6b with ok=3.5, single change. FAIL branch (iii) nothing moves -> band lever refuted for this lineage. Per-leg check in verdict: swing count/leg (clone ~30, cheat ~22), stride_m_mean (clone ~0.034, cheat ~0.042), leg5 duty (clone ~0.52, cheat ~0.57) — the fix should move these back toward the clone cluster. NO DOWNLOAD_ANSWER change from this run.

**verdict**: FAIL -- gate breaks on (c) slip/m only: det slip_per_m 3.04 vs frozen clone control 1.89 = 1.611x clone (cap 1.15x), a hair WORSE than phasedir5's 1.590x despite retightening loadslip_ok 7.0->3.0 / loadslip_max 10.0->6.0. Zero falls DR-0+own-DR det+sto (gait_valid 6/6 in all four eval sets); progress 0.961x clone PASS (cap 0.9x); dir_err 28.45deg vs clone+5=40.5deg cap PASS; speed_mean 0.067 in [0.06,0.096] PASS; policy_std held 0.131 both passes (warm-log-std-override intact). Pre-registered branch (iii) fired: W&B env/walk_loadslip_ratio held 4.2-4.7 the entire 2M-step run (never fell below ~4, the SAME range phasedir5 saw with the old unpriced 7/10 band) even though walk_loadslip_factor DID engage harder this time (0.99 -> 0.48-0.56, confirming the tighter thresholds raised the charge actually collected) -- the extra price changed what was PAID, not the policy's BEHAVIOR. Per-leg signature is UNCHANGED from phasedir5's cheat family: swings/leg 19.3-25.3 (clone ~30), stride_m_mean 0.0435 (clone 0.0335), duty-skewed legs (idx1/3/5 ~0.54, idx0/2/4 ~0.44-0.49; clone uniform 0.50-0.54). ROOT CAUSE: the episode-cumulative-ratio pricing (walk_loadslip_gate + k_loadslip_excess) is dominated by the anchor/course/height reward terms at any (ok,max) pair explored so far (7/10, then 3/6) -- the BAND VALUE lever is refuted for this lineage exactly as the gate's own fork predicted; retuning ok/max again would not be informative. ACTED ON (no new code needed): found the prescribed fix ALREADY BUILT and BANK-TESTED but never enabled in this lineage -- reward.k_drag_stance (test_drag_stance_stack_prices_skating_below_stepping, landed 2026-08-11, re-confirmed passing this cycle) prices per-FOOT per-STANCE accumulated drag directly in mm, not diluted by episode-length progress the way the ratio is; calibrated operating point k=8000/m over a 6mm per-stance allowance (0.25mm/tick jitter floor). Validated with a matched-env pod pricing A/B before committing budget (logs/ckpt_eval/pd7_dragstance_ab_{clone,drag}, det, dr-scale 0, identical full phasedir6 cfg-set stack + only this one new charge added): the honest phase1 BC clone (slip/m 1.89) scores -1868 vs the phasedir6 cheat checkpoint (slip/m 3.04) scoring -3971 -- a >2x swing IN FAVOR of the honest gait, matching the original 08-11 audit's calibration claim (skater's drag ~2.4x its income). Launched cw-dep-bcgait4-phasedir7-dragstance (single change: add reward.k_drag_stance=8000.0 / drag_stance_allow_mm=6.0 / drag_stance_tick_floor_mm=0.25 onto the phasedir6 stack, nothing else touched) as the direct training-time test of this already-proven mechanism.

