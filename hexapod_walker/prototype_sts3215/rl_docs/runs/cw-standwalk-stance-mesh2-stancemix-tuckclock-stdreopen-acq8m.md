# cw-standwalk-stance-mesh2-stancemix-tuckclock-stdreopen-acq8m

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-26T01:20:01+00:00

**pod**: hexapod-mjx-train-4

**steps**: 8000000

**parent**: cw-standwalk-stance-mesh2-stancemix-tuckclock-stdreopen

**wandb_id**: 5xk1serz

**hypothesis**: Is the stdreopen mix recipe's 2M flat-start press-up pin a budget problem, exactly as the isolated riseonly tuckclock recipe's was? The isolated arm (riseonly-bcchain3-meshref-tuckclock-acq8m) showed the SAME 2M signature (reward falling through a trough: -58 at 2M, bottom -198 at 3M) and then fully solved flat rise by 8M (12/12 valid_plant, reward +1570) once its std anneal (0 -> -4 over the first half) had room to complete and pay off. The stdreopen 2M canary pair just reproduced that exact mid-trough state in the 3-way hold/rise/lower mix (reward quarters 4.9/-37.2/-55.8/-109.4, flat probe 12/12 OC-pinned at 2.64A, but hold 6/6+6/6 and lower 5/6 intact) and FAILED its canary on the pre-registered budget route. This arm is that route: the EXACT stdreopen recipe (warm from stancemix_bcchain3_stdanneal, mesh ref + flat-time-indexed BC-anchor clock, log-std 0 -> -4 anneal-frac 0.5, hold=0.1/rise=0.45/lower=0.45), only steps 2M -> 8M, so the anneal stretches to 4M mirroring the isolated arm's schedule. Prediction-if-true: reward turns upward by ~3-4M and the flat probe closes toward valid_plant by 8M with hold/lower unregressed. Prediction-if-false: flat probe herr/OC-pin budget-invariant vs 2M or reward still falling at 6M - the mix context itself blocks flat-rise acquisition (interference), and the next lever is sequencing (acquire flat rise riseonly-first, then anneal the mix in) NOT more budget or another config key.

**gate**: ACQUISITION (8M, joint 2-seed pair with -acq8m-s1): PASS if flat-pinned probe (goal.rise_flat_frac=1.0/partial=0/rsi=0, det+sto 6+6, DR-0) reaches >=10/12 valid_plant per seed with no majority 2.64A OC-pin, AND hold det+sto >=5/6+5/6 zero-term, AND lower >=5/6 honest (<=10mm herr) per seed -> this becomes THE mesh stance mix checkpoint; next is STAND_HEIGHT rungs 4-5 / walk distill on top. FAIL if the flat probe pin is budget-invariant (herr/OC signature ~unchanged vs the 2M canary) in both seeds or reward still falling at 6M -> mix-context interference is structural; next lever is SEQUENCING (riseonly flat acquisition first, then re-introduce hold/lower mix on the solved ckpt), explicitly NOT more budget and NOT another single-key respec.

**verdict**: ACQUISITION FAIL (seed 0 of the joint pair, own reading — s1 out of scope this cycle, left running per assignment): 8M budget does NOT close the flat-start press-up pin in the 3-way hold/rise/lower mix, even though the identical recipe (mesh-native scripted rise ref + flat-time-indexed BC-anchor clock + reopened std 0->-4 anneal) fully SOLVED flat rise in isolation (riseonly-bcchain3-meshref-tuckclock-acq8m: 12/12 valid_plant by 8M). Evidence: flat-pinned probe (goal.rise_flat_frac=1.0/partial=0/rsi=0, det+sto 6+6, DR-0) run by hand on train-4 -> 2/12 valid_plant (10/12 over_current-terminated, all at the exact 2.64A press-up ceiling, swing_count ~0 on the failing episodes, height_err_end 6.8-39.7mm not settling) -- barely moved off the 2M canary's 0/12, nowhere near the >=10/12 PASS bar, and the video (rise_sto_0/1 frame strips) shows the same splayed flat-on-ground press-up with no belly lift, terminating on over_current -- a real but tiny improvement (0->2/12), not the trajectory-matching payoff the isolated arm showed at the same budget. The OTHER two gate clauses are clean: standard DR-0 gate hold 6/6+6/6 zero-term, lower 6/6+6/6 zero-term, non-flat rise det 6/6 (bridge/crouch/rsi draws) + sto 4/6; own-DR 0.2 hold 6/4, rise 3/2, lower 6/4 -- hold/lower transfer intact, only the flat-rise clause is broken. Reward rose sharply all run (quarters -52.0/174.9/764.5/1267.5, mirroring the isolated arm's own recovery shape) -- per the 08-21 ruling this is the MISALIGNMENT-vs-undertrained question, and the run's own pre-registered FAIL branch already named the answer: 'flat probe pin budget-invariant vs the 2M canary in both seeds -> mix-context interference is structural; next lever is SEQUENCING (riseonly flat acquisition first, then re-introduce hold/lower mix on the solved ckpt), NOT more budget.' That is exactly what this data shows for seed 0: the reward gain bought hold/lower quality and non-flat rise, not the flat-start clause the gate cares about. NEXT (queued this cycle): SEQUENCING respec -- warm-start the exact stdreopen 3-way mix recipe from the ALREADY-SOLVED riseonly-bcchain3-meshref-tuckclock-acq8m checkpoint (12/12 flat valid_plant) instead of from stancemix_bcchain3_stdanneal, so hold/lower training builds on top of a flat rise that already works instead of re-deriving it under competing reward pressure. Joint call on the pair formally stays open for whichever cycle reads -acq8m-s1 (left running/untouched this cycle per assignment), but given seed-0's own budget-invariant reading matches the pre-registered fail signature closely, SEQUENCING is queued now rather than waiting idle.

