# cw-standwalk-stance-mesh2-stage2-dualbc1-anchor14-walkretaincoef1-rescue

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY PASS (own scope) - joint pending -s1

**created**: 2026-08-27T17:27:09+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-standwalk-stance-mesh2-stage2-dualbc1-anchor11-walkretain

**wandb_id**: o4evhjg7

**hypothesis**: Plain English: anchor11-walkretain's own seed0 read this cycle (not the catastrophe seed, but a real cost: prog_ratio 0.23->0.08, slip/m 6.56->13.64) showed the SAME shared coef=3.0 dose taxing walk on the RESCUE recipe too, mirroring anchor12's control on the other recipe family. This arm is anchor11-walkretain's exact recipe (init-from anchor6b-logstdsplit-fix's checkpoint, the hold-fixing/walk-catastrophe-prone pair) with ONE change: train.bc_anchor_walk_coef=1.0 instead of the shared 3.0. The decisive question this recipe alone can answer (unlike anchor13's control-recipe twin): does the LIGHTER decoupled dose still rescue seed1's real 0/6 gait_valid collapse, or does the anchor need the heavier dose to overpower the catastrophe -- i.e. is there a dose window that rescues seed1 without taxing seed0 the way coef=3.0-shared did?

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY at 2M, joint call with -s1 twin; matched controls = anchor6b-logstdsplit-fix{,-s1} (the untouched parent: seed0 clean prog_ratio 0.23, seed1 catastrophe gait_valid 0/6 sacrificed [0,1,2,4,5]) AND anchor11-walkretain{,-s1}'s own coef=3.0-shared numbers. WIRING CHECK: train/bc_anchor_loss_walk + train/bc_anchor_fill_walk nonzero. RESCUE-PASS if seed1 (the catastrophe target) shows gait_valid>=5/6 with zero sacrificed legs AND seed0 stays close to its OWN parent's clean band (prog_ratio >=0.18, i.e. most of anchor11-walkretain's cost recovered) -- the dose window exists, fund a 2x-seed acquisition wave. RESCUE-PARTIAL if seed1 improves (gait_valid up, fewer sacrificed legs) but doesn't fully clear the bar, or seed0's cost isn't recovered -- fund a middle dose (e.g. 2.0) next. FAIL if seed1 still shows the anchor4-class collapse (gait_valid 0-1/6, sacrificed legs) at this lighter dose -- the walk anchor mechanism itself cannot rescue this catastrophe class at any dose tried so far; escalate to the per-mode-group objective normalization item (-2.35a) or the reward/task-level audit. Read reward trend per 08-21 first.

**verdict**: CANARY PASS (own scope) - joint pending -s1. Plain English: on the recipe's already-clean seed (not the catastrophe target), the lighter per-mode walk-anchor dose (coef=1.0, decoupled from stance's 3.0) recovers almost all of the walk-quality cost that the shared coef=3.0 dose (anchor11-walkretain) imposed on this exact lineage. Evidence: wiring check first PASS (train/bc_anchor_loss_walk + fill_walk nonzero every rollout). DR-0 gate det walk: gait_valid 6/6, sacrificed_legs=[] on ALL 6 episodes, progress_ratio 0.178-0.181 (med 0.18), slip/m 6.07-6.80 (med ~6.2) -- vs the untouched parent anchor6b-logstdsplit-fix's own clean DR-0 band (prog med 0.23, slip med 6.57, gv 6/6) and vs anchor11-walkretain's coef=3.0-shared cost on this SAME seed (prog 0.08, slip ~13.5, gv 6/6 but heavily taxed): prog recovers 0.08->0.18 (67% of the cost span back toward the parent) and slip is FULLY recovered (13.5->6.29, matching/beating the parent). This clears this run's own pre-registered seed0 clause exactly ("prog_ratio >=0.18, i.e. most of anchor11-walkretain's cost recovered"). Own-DR(0.5) is stronger still (det walk prog 0.25, slip 5.14, gv 6/6). Video (contact sheet + walk_det frame strip) shows a genuine six-leg alternating-tripod gait, upright, no drag/sacrifice -- matches the numbers. Non-walk modes are the recipe's known pre-existing residual, unchanged by this lever: rise 4/6 det+4/6 sto, lower 6/6 det+5/6 sto, hold 4/6 det+4/6 sto (own-DR similar) -- not new damage. NOTE FOR THE JOINT READER: the concurrently-verdicted CONTROL pair at the same coef=1.0 (anchor13-walkretaincoef1-base{,-s1}, on the walk-clean anchor2 lineage, ceiling prog 0.38) also landed at prog~=0.18 det walk (seed0: 0.13->0.18, only 20% of ITS cost span recovered; seed1 inverted/worse) -- i.e. two different lineages under the identical coef=1.0 dose both converged to approximately the SAME absolute prog_ratio (~0.18) despite very different starting ceilings (0.23 vs 0.38). That coincidence is worth flagging for whoever runs the reward/task-level audit: it reads more like an absolute anchor-imposed ceiling on this metric than a clean proportional-dose recovery, so do not read my seed0 clearing its bar as proof the coef=1.0 mechanism is dose-clean in general -- it is specific evidence for THIS lineage/seed only. Why: the walk teacher's own training dose (1.0) is close to sufficient to retain SOME of a fragile walk's quality on this exact seed/lineage without the shared coef=3.0's heavy tax, consistent with -2.35(b)'s prediction. What's next: this run's own gate is a JOINT 2-seed call -- the decisive half is anchor14-walkretaincoef1-rescue-s1 (the actual seed1 0/6-gait_valid catastrophe target), which is still off-limits to this cycle (assigned elsewhere / still finishing). Do NOT fund a 2x-seed acquisition wave or declare RESCUE-PASS/PARTIAL/FAIL until -s1's own harness numbers (gait_valid, sacrificed_legs) are read against anchor11-walkretain-s1's own already-closed rescue shape (0/6->6/6 gv, prog 0.01->0.16) -- if -s1 clears >=5/6 gv with zero sac legs, this is a genuine RESCUE-PASS/PARTIAL pending seed0's own bar (met here); if -s1 still shows the anchor4-class collapse, this is FAIL alongside anchor13's control despite seed0's clean read, per the gate's own text ('FAIL if seed1 still shows the anchor4-class collapse... at this lighter dose').

