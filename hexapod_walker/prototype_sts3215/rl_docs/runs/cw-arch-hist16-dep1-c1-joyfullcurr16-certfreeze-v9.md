# cw-arch-hist16-dep1-c1-joyfullcurr16-certfreeze-v9

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-24T23:33:45+00:00

**pod**: hexapod-mjx-train-5

**steps**: 40000000

**parent**: cw-arch-hist16-dep1-c1-joyfullcurr14-certfreeze-v8

**wandb_id**: zxoxwg3i

**hypothesis**: Plain English: certfreeze-v8 (verdicted this cycle) proved the bucket-scope fix works -- frontier leaves b1, reaches b3 (side90 opens) -- but then plateaus AT b3 for the rest of the 40M-step run with the identical stuck-stop-cert signature v7 showed at b1. Root cause (code-read, this cycle): the cert-time hold supervisor (_walk_stop_freeze_override) exempts any tick with wz_ref!=0, but a wz-diet bucket's stop_frac and wz_zero_frac draws are independent rng calls on the SAME resampled segment, so ~half of a nominal 'stop' segment's ticks also carry wz!=0 in any bucket carrying the diet (side90_20s onward) and are therefore exempt from the very freeze meant to clear the 0.015 m/s bar, while the legacy stop_speed_m_s metric counts every such tick anyway -- structurally guaranteeing the cert can never clear regardless of policy quality. This run tests the ONE fix: --walk-curriculum-version 8->9 (WALKCURR_BUCKETS_V9, byte-identical bucket scope/diet to V8 -- built+tested this cycle, test_walk_curriculum.py 56/56 incl. 4 new V9 tests, test_walkcurr_mjx.py 19/19, test_walk_stop_settle_metric.py 8/8 incl. 2 new stop_speed_pure_m_s tests, test_launch_run_control_hz.py 8/8 new -- except every gated bucket is certified on the new stop_speed_pure_m_s metric instead, which excludes the same wz!=0 ticks the freeze does, so the cert and the freeze finally agree about what counts as 'commanded still'. Pinned to the SAME legacy control.hz=25 as its v8 parent (via the newly-built --allow-legacy-control-hz escape hatch -- CURRENT_TRUTHS documented this as already landed 08-24 but it was never actually wired into launch_run.py, discovered+fixed this cycle) to isolate this lever from the separate, concurrently-running 100Hz rate-conversion question (joyfullcurr15-v8-hz100-r1/r2).

**gate**: PASS: frontier promotes past b3 to at least b5 (rear135 opens) AND held-out 60s joygate falls <=2/48 AND DR-0 det gait_valid stays 6/6 no leg sacrifice. PARTIAL: frontier promotes past b3 but joygate/DR-0-gait doesn't fully clear (genuine trade, or improvement without clearing). FAIL: frontier still stalls at b3 (the cert-metric fix didn't touch the mechanism, or turning-in-place genuinely induces real translational drift the freeze can't suppress) -- points next at building a dedicated turn-in-place drift/stillness gate instead of a metric-alignment fix.

**verdict**: FAIL per its own pre-registered gate, but the best-behaved checkpoint in the V6-V9 certfreeze ladder to date. Reward healthy full 40M (ep_rew_mean 1117, quarters 1174.9/1307.8/1171.6/1380.8, fps 4696). walkcurr/frontier stalls at b3 (side90_20s) exactly like v8 -- the V9 fix (stop_speed_pure_m_s, excludes wz!=0 ticks from the stop-tick accumulator) fires correctly as designed (stop_ticks_pure_frac 0.884 vs 1.0, confirming it now actually excludes turn ticks) but the PURE stop speed still reads 0.0231 m/s at b3, still over the 0.015 cap -- this is no longer a measurement-confound artifact, it is a real residual translational drift during nominal turn-adjacent stop segments that the freeze supervisor can't suppress. This is the run's own pre-registered second FAIL branch (turning-in-place genuinely induces real drift), not the first (metric still confounded). Held-out 60s joygate: falls 3/48 (cap<=2, tied with v7's 3/48, better than v8's 4/48 and hz100-r2's 8/48), slip/m 2.269 (cap 2.9, clean), dir_err med 48.03deg (allow 40, close miss), gait_valid_frac 0.9375 -- per-leg duty spread balanced ([0.495,0.575,0.405,0.625,0.43,0.595], sacrificed_frac all <=0.021) with NO leg-sacrifice fingerprint anywhere in the 48-episode panel, the first joygate read in this entire ladder to be completely clean of the recurring leg-lock pathology. DR-0 gate: det gait_valid 6/6 (prog_ratio 0.93, slip/m 1.40, dir_err 25.9deg, roll peak 5deg) -- clean, meets the gate's own named DR-0 clause; sto gait_valid 5/6 sac=[2,4] (prog 0.44, slip 10.02). Own-DR(0.5): det gait_valid 5/6 sac=[3] (prog 0.82, slip 1.99), sto gait_valid 6/6 (prog 0.56, slip 1.93). Video (both gate and owncfg contact sheets) confirms real six-leg cycling, level body, no static/frozen pose. Net: this is closer to the full PASS bar than any prior ladder member (falls 3 vs cap 2, dir_err 48 vs cap 40 -- both near-miss, not the 6-8 falls/50+deg misses of v7/v8/hz100-r2) but the b3 stop-cert genuinely does not clear even with the metric fix. Per the certfreeze-v8 dig-in's own pre-registered fallback: since the metric-alignment fix (item 1) is now proven insufficient, the remaining lever is item 2 -- a dedicated turn-in-place drift/stillness charge or gate (not another metric fix), which needs its own scripted twin + bank proof before launch. Not built this cycle (scope: a new reward mechanism requiring semantics-bank proof, better suited to a dedicated build cycle given 5 other joystick arms already in flight this cycle: v8-hz100-r2, both mesh acq1 arms, and the concurrently-launched gaitgate-cont1/-scratch1 pair testing the separate leg-sacrifice lever). Champion unchanged (stotight45-seed13); DONE gate stays met per 08-23; V6-V9 ladder remains operator-ordered hardening, not gate-blocking.

