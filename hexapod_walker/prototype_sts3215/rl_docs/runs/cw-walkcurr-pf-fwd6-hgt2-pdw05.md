# cw-walkcurr-pf-fwd6-hgt2-pdw05

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-24T00:04:31+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-walkcurr-pf-fwd6-hgt2

**wandb_id**: 725hv284

**hypothesis**: Plain English: hgt2 (tight height-gate dose) sat completely flat because of a bug, not a bad hypothesis -- the safety grace period (2.0s) exactly matched the default park-duty averaging window (2.0s), so the one existing charge that already prices a permanently-airborne leg never got a chance to fire before termination reset it. Single lever vs hgt2: goal.park_duty_window_s 2.0->0.5 (fills 4x before the grace boundary), everything else byte-identical (sigma=11mm/drop=25mm/grace=2.0s, k_park_duty unchanged at 0.08). Bank-proven this cycle (test_walkcurr_pf_hgt_* now parametrized 4 doses incl. this one, 16/16 green: gait/park/stall clearly beat belly_sit, honest gait untaxed, ranking undisturbed). Also directly answers this cycle's OTHER finished arm, hgt1 (loose dose): hgt1's own-cfg gate shows the policy converging on a NEW static tripod-ish pose (3 legs duty~0.97, 3 legs duty~0.03, height_err~41mm) that sits comfortably UNDER hgt1's loose 60mm cutoff -- this arm's TIGHT 25mm cutoff is well below that 41mm settling point, so a de-confounded park_duty charge at the tight cutoff tests whether the SAME escape is blocked once both the threshold is tight enough and the charge can actually fire. Prediction-if-true: walk_freeprog_score crosses toward/past 0, gait_valid>=4/6 on video. Prediction-if-false (same flat/static signature): the confound was real but not sufficient, escalate to a direct minimum-total-foot-contact charge (new mechanism, needs its own scripted twin) before BC-kickstart.

**gate**: Same rung-1 gate as hgt1/hgt2: C-env det fixed-forward panel -- prog_ratio>0 and gait_valid on >=4/6 det episodes with visible forward travel on video, env/walk_freeprog_score leaves [-0.10,-0.05] and trends toward/past 0 by 2M, clip_fraction stays healthy (no crush). PASS = rung-1 lands, move to rung 2. FAIL with reward_park_duty now nonzero but still flat/static = confound fixed but dose insufficient (read jointly with the -pdx15 sibling). FAIL with reward_park_duty still exactly 0 = confound diagnosis was wrong, re-audit.

**verdict**: Confound-fix arm FAILS the rung-1 gate on the SAME underlying trajectory as every prior height-gate arm, even though the fix itself worked mechanically. Evidence: env/reward_park_duty is now nonzero throughout (-0.003 -> -0.011, was EXACTLY 0 all run on hgt2) confirming goal.park_duty_window_s=0.5 does fill before the 2.0s grace and the charge fires as designed. But env/walk_freeprog_score never leaves [-0.11,-0.10] (ends -0.098, no improvement over hgt2/hgt1), env/walk_speed decays 0.10->0.054 m/s, env/height_err_mm climbs monotonically 11->51mm the same way hgt1's did, and clip_fraction dips to near-zero mid-run (2.6e-5) before a partial recovery (0.029) -- reward and eval both non-improving with adequate budget = genuinely stuck, not misaligned, per the 08-21 ruling. Own-cfg gate: 6/6 det + 6/6 sto episodes terminate walk_low_height (the tight 25mm cutoff now firing every time, height_err_end 63-95mm), gait_valid now TRUE with duty spread 0.12-0.84 (no more sacrificed legs, so the tripod-lock escape specifically IS closed) but forward_dist is 0.007-0.03m/25s and slip_per_m 5.8-7.8 -- video (walk_det_0.png) shows a slow progressive splay-and-sink over the episode into a wide low crouch, zero net translation, then the safety cutoff ends it. Why: de-confounding the charge changed WHICH static pose the policy settles into (rigid tripod-lock -> splayed slow-sink) but not WHETHER it settles into one; the underlying survive-motionless-beats-risk-a-fall economics this whole campaign has been probing is untouched by this lever. What's next: read jointly with the pdx15 dose sibling (also FAIL, same signature) -- this closes the park_duty-class charge (any bank-legal dose, confound fixed or not) as a mechanism for unlocking rung-1 discovery. DIG-IN flagged for the next escalation choice (direct minimum-total-foot-contact charge, a new mechanism needing its own scripted twin + bank proof, vs. finally flagging the BC-kickstart last resort to the operator) since every other named lever (RND dose+budget, rung-0 swing-income, GRU, gSDE, rscale dose+continuations, height-gate dose+confound) is now closed.

