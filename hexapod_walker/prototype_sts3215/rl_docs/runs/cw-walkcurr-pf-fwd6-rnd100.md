# cw-walkcurr-pf-fwd6-rnd100

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-23T22:42:50+00:00

**pod**: hexapod-mjx-train-1

**steps**: 2000000

**parent**: cw-walkcurr-pf-fwd6-rscale50

**wandb_id**: 58wtkxxr

**hypothesis**: Plain English: rnd02 (coef 0.02) and rnd10 (coef 0.10, 5x) both FAILED this cycle with NEARLY IDENTICAL rnd/intrinsic_mean decay curves -- a 5x dose change produced almost no measurable behavior difference, so this is a dose-INSENSITIVE freeze in that range, not a dose-starved one a small bump would fix. This arm jumps an order of magnitude further, --rnd-coef 1.0 (50x rnd02, 10x rnd10), to test whether the intrinsic bonus needs to be comparable in scale to the FULL per-step task reward (not just the small rung-1 walk charges, which are themselves only 0.01-0.09 at this x0.02-scaled recipe) before it can out-compete the static crouch's own charge-avoidance income. Same exact fwd6-rscale50 diet otherwise, single lever. Pure exploration mechanism, no bank re-proof needed. Prediction-if-true: walk_freeprog_score crosses 0 within 2M, det gait_valid panel shows six legs cycling; rnd/intrinsic_mean should now show a VISIBLY DIFFERENT trajectory from the 0.02/0.10 pair (not just a rescaled copy) if the policy is actually exploring differently. Prediction-if-false (frozen again, AND rnd/intrinsic_mean still tracks the same shape as rnd02/rnd10): RND-as-a-class is refuted at bank-legal reward scales on this recipe regardless of dose -- declare the rung-1 from-scratch-MLP recipe fully refuted at 2M under every tested exploration mechanism (noise-scale, entropy, gSDE, GRU, reward-rescale, RND at 3 doses -- 12 total arms) and escalate to the track's last-resort item (d), a brief BC kickstart (which brushes the 'no BC teacher' rule but is now the only untried item on the pre-registered order). Strongest alternative: coef=1.0 overshoots so hard the policy chases pure novelty and never settles into a task-relevant gait even if it does move -- readable on video (chaotic flailing vs rhythmic stepping) and by comparing det gait_valid/slip against rnd02/rnd10.

**gate**: Same rung-1 gate as rnd02/rnd10: prog_ratio>0 + gait_valid>=4/6 det with visible forward travel on video, walk_freeprog_score crosses 0, clip_fraction stays healthy (no collapse). FAIL alongside rnd02/rnd10 (3/3 doses spanning 50x, all frozen) closes the RND lever on this recipe -- next is BC-kickstart, not a 4th dose point.

**verdict**: RND at 50x the original dose (rnd_coef=1.0, order-of-magnitude jump past rnd02/rnd10) STILL does not unfreeze the rung-1 diet -- gate FAIL, 0/6 det gait_valid, all six legs sacrificed (duty ~0.02 uniformly), prog_ratio ~0.00, slip/m 6.3-8.9. Unlike rnd02 vs rnd10 (near-identical optimizer stats), this dose DID move the optimizer: clip_fraction reached 0.12-0.17 (vs 0.03-0.08 at lower doses) and std grew further (0.368->0.380) -- real behavioral perturbation -- yet walk_freeprog_score ended WORSE (-0.020) than both lower doses (-0.005/-0.007), and rnd/intrinsic_mean's decay curve is STILL nearly the same shape as rnd02/rnd10 (0.0367->0.0061) despite the much bigger optimizer response -- the extra churn did not translate into genuinely novel STATES, just noisier movement within the same basin. CROSS-ARM STRUCTURAL FINDING (the headline result of this dose sweep): height_err_end_mm=116.3mm EXACTLY, on every det episode, in ALL THREE of this cycle's RND arms (rnd02/rnd10/rnd100) -- and matches, to the same 116.3mm, two independent concurrent-cycle arms on a DIFFERENT diet entirely (rung-0 swing3+RND at coef 0.02 and 0.06) plus a same-recipe replicate (fwd6-rscale50-rnd1). Six independent arms, three RND doses spanning 50x, two different task diets (rung-0 sub-goal vs rung-1 fixed-forward), all converge on the IDENTICAL collapse height and near-identical duty/pose signature -- this is not a per-run coincidence, it is a shared attractor in the BASE (non-walk-specific) reward stack that no exploration-mechanism dose can out-compete. Why: reward aligned + optimizer demonstrably NOT crushed at this dose (highest clip_fraction of any rung-1 arm ever) + still lands in the identical basin = the blocker is not exploration strength at all, it is a REWARD-SHAPE defect (a specific height/tilt pricing gap letting a low, splayed 'belly-sit' pose collect enough base income to beat any explored alternative) -- a genuine root-cause-needed finding per the 08-21 ruling's 'reward misaligned, fix and relaunch' branch, not a 4th dose or a BC-kickstart. Next: dig into WHY 116.3mm specifically is stable (what height/tilt termination and k_height=2.0 actually enforce at this scale) and reprice/re-terminate the base reward before spending any more exploration-mechanism budget -- flagged DIG-IN this cycle.

