# cw-arch-hist16-dep1-c1-joyfullcurr14-certfreeze-v8

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: PASS (partial)

**created**: 2026-08-24T20:12:59+00:00

**pod**: hexapod-mjx-train-5

**steps**: 40000000

**parent**: cw-arch-hist16-dep1-c1-joyfullcurr13-certfreeze-v7

**wandb_id**: tvps19cg

**hypothesis**: Plain English: V7's turn/reversal diet was meant to harden the WIDE-heading buckets (side90+) against the joygate's stress_mix falls, but it also landed on the still-front-cone b1/b2 rungs, whose own stop-settle cert then never passed again -- the frontier got stuck at b1 for the entire 40M-step v7 run and the wide-heading hardening never trained at all. This run tests the scope-fixed V8 table (identical diet, starting at side90_20s instead of front45_20s) with the exact same warm start/cert-freeze/reward recipe as v7, held at v7's own 25 Hz control rate (--allow-legacy-control-hz; see OPERATOR_QUESTIONS.md 08-24 ~20:0x -- this keeps the comparison to v7 single-lever, since a concurrent cycle is separately still debugging this same lineage's 100 Hz warm-start rate conversion). Single lever: --walk-curriculum-version 7 -> 8. Prediction-if-true: frontier promotes past b1 again (like v6's b0->b5) AND the held-out joygate improves toward v6's own bar (falls <=2/48) without the front45 stop-creep regression v7 showed. Prediction-if-false: if frontier still stalls despite the scope fix, the stop-gate itself (0.015 m/s in a 20s window) is too tight for ANY reversal-bearing bucket regardless of which rung it starts at, pointing next at a settle-time/grace-window fix rather than bucket placement.

**gate**: PASS: frontier promotes past b1 to at least b3 (side90 opens) AND held-out 60s joygate falls <=2/48 AND DR-0 det gait_valid stays 6/6 with no leg sacrifice. PARTIAL: frontier promotes past b1 but joygate/DR-0-gait doesn't fully clear (genuine trade, or improvement without clearing). FAIL: frontier still stalls at b1 (the scope fix didn't touch the mechanism) -- points next lever at the stop-settle grace window itself, not bucket placement.

**verdict**: PARTIAL, matches the ledger's own pre-registered branch exactly. Result: the V8 bucket-scope fix (turn/reversal diet now starts at side90_20s instead of front45_20s) DOES unstick the frontier from b1 -- promotions climbed 1->2->3 in the first ~1000 cert rounds (b0/b1/b2 all pass instantly off the warm start, side90_20s/b3 opens) -- but the frontier then plateaus AT b3 for the remaining ~76 cert rounds (near the entire 40M-step budget), never promoting to b4 (side90_60s). b3's own stop_speed_m_s sits 0.023-0.042 m/s the whole run (cap 0.015), never trending down -- the SAME stuck-stop-cert signature v7 showed at b1, just relocated one rung up, confirming the run's own pre-registered if-false branch: bucket placement was not the mechanism, the stop-settle bar itself is too tight for any bucket carrying the wz/reversal diet. Held-out 60s joygate: falls 4/48 (cap <=2/48, v7 was 3/48 -- flat/slightly worse, not improved), dir_err med 47.49deg (allow 40), slip/m 2.268 (cap 2.9, clean), gait_valid_frac 0.9375. DR-0 gate: det clean (0 terms, gait_valid 6/6, no leg sacrifice, prog 0.89, slip/m 1.35, roll peak 4deg) -- the gate's named 'DR-0 det gait_valid 6/6 no sacrifice' clause IS met; sto sacrifices legs [2,4] (not named in the gate text). Own-DR(0.5): det sacrifices legs [3,4] + roll peak 23deg (getting close to the 25deg safety cutoff, though 0 actual terms across all 4 passes/24 episodes); sto clean 6/6. Video (owncfg det0/det3 contact sheets): clean upright six-leg cycling, no topple, consistent with 0 falls. ROOT CAUSE (traced this cycle, code-read not just inference): the cert-time freeze supervisor (goal.walk_stop_freeze_s, wired via --walkcurr-cert-cfg-set) explicitly EXEMPTS any tick where wz_ref!=0 (sim_env._walk_stop_freeze_override, 'a nonzero turn IS the commanded motion') -- correct for genuine turn-in-place segments. But WALKCURR_BUCKETS_V8's side90_20s (b3) draws wz independently of the linear-stop draw (walk_task.py _sample_walk_curr: stop_frac and wz_zero_frac=0.5 are separate rng.random() calls on the SAME resampled segment), so ~half of the bucket's nominal 'stop' segments (vx=vy=0) ALSO carry wz!=0 and are therefore exempted from the freeze, while the cert's own stop_v_sum/stop_ticks accumulator (_walk_probe_summary) counts ALL vx=vy=0 ticks toward stop_speed_m_s regardless of wz. Freeze-ON vs freeze-OFF numbers from the earlier stopfreeze-probe (0.0133 vs 0.0326 on b1, which has wz_max=0 and is therefore 100% frozen) bracket b3's observed 0.023-0.042 almost exactly as the average of a half-frozen / half-unfrozen population. This is a genuine mechanism/metric mismatch (the freeze and the cert gate disagree about what counts as 'commanded to be still'), not dose-insensitivity or a seed issue -- it structurally guarantees b3+ can never clear 0.015 regardless of training quality, for as long as the bucket trains wz on top of a linear stop and the cert measures stop_speed_m_s without excluding wz!=0 ticks the same way the freeze does. DONE gate stays met via stotight45-seed13 (08-23); this V6/V7/V8 ladder remains operator-ordered hardening, not gate-blocking. NEXT (specified, not built -- DIG-IN, see final message): align the cert's stop-tick accumulation with the freeze's own wz-exemption semantics (or build a genuinely separate turn-in-place stillness/drift gate) before any V9 relaunch; needs its own bank-proof per the reward/task-mechanism launch rule, not a triage-budget edit. Evidence: logs/ckpt_eval/cw_arch_hist16_dep1_c1_joyfullcurr14_certfreeze_v8_{gate,owncfg,joygate}/, W&B run tvps19cg, logs/experiments/cw-arch-hist16-dep1-c1-joyfullcurr14-certfreeze-v8/wandb_history.csv (walkcurr/frontier, walkcurr/b3_side90_20s/*).

