# cw-dynrep-criticD-walkcurr4-bridge1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: REFUSED

**created**: 2026-08-18T11:02:11+00:00

**pod**: hexapod-mjx-train-9

**steps**: 4000000

**parent**: cw-dynrep-criticD-walkcurr4-gaitinit-hard1

**hypothesis**: Teach the walk-champion critic-D wiring to acquire a real gait by starting training already at the actor's own comfortable speed instead of a foreign command band, so a fresh critic can adapt to a competent walker instead of erasing it. Operator evidence-based correction fb_20260818T102844_116d4c (the tournament's own pre-registered next step) after all prior 4M canaries converged on the same crouch-park signature: canA-r2/canB-r1/canC-r1 (scratch actor, any LR/gates) and gaitinit-bcinit/-hard1 (actor-only transplant from the proven scripted-gait/gait-hardened checkpoint under V2's 0.08-0.12 m/s ignition band) ALL failed with 4-6/6 legs parked -- bcinit specifically kept posture/safety (height_factor 0.76, zero training falls) but LOST commanded travel (cmd_prog_frac 0.0575 final) chasing a speed band the source checkpoint never walked at. Causal combo now applied together (six coupled changes, operator-ordered, multi-variable bundle permitted 2026-08-15): (1) actor-only init from the GAIT-HARDENED ppo_goal_cw_dep_bcgait1_hard1.zip (10M RL-tuned, height -8.5..-9.8mm, slip 1.27-1.54/m, zero falls) rather than the raw BC clone; (2) NEW WALKCURR_BUCKETS_V3 "bridge" curriculum whose first two rungs sit at the SOURCE'S OWN operating point (dead-straight 0.05-0.06 m/s then 0.05-0.10 m/s, no heading jitter/resample/stops, DR0) before falling back to V2's existing direction/heading ladder verbatim (joystick goal preserved); (3) NEW --actor-freeze-steps 500000 (update_health.set_actor_freeze, default-off): the transplanted actor's param group runs lr=0 for the first 0.5M steps so the fresh condition-D critic can adapt to it without the early high-variance value error erasing the actor, then releases into a gentle 5e-5 x3-epoch update (was 2e-4 x5); (4) continuous height/progress income gates stay ON (reward.walk_height_gate/walk_kernel_prog_gate=1) but the hard height-drop TERMINATION is OFF during this arm (safety.walk_max_height_drop_mm=0) so a momentary dip during the frozen bridge cannot kill the episode before the critic adapts; (5) NEW --walkcurr-cert-at-init (default-off): runs the bucket-0 deterministic cert on the untrained transplant BEFORE any PPO update and logs it (walkcurr/pre_b0_*), aborting on a falling or non-tracking init rather than training over a broken transplant/obs mapping; (6) pre-launch mechanism smoke (train-9, 100k-step budget, --walkcurr-precert-only) already ran this exact cert and PASSED CLEANLY: prog=1.164, falls=0, slip/m=0.93, roll=5.7deg, height_factor=0.83, slew=0.90 -- the transplant is provably adjacent to its own source skill under the new obs/curriculum wiring before spending any of the 4M budget. Prediction-if-true: B0 bridge promotes by <=1M and by 4M frontier>=B2 with cmd_prog_frac>=0.6, height_factor>=0.8, slip<=2, zero falls (the operator's own gate). If-false: even a frozen critic adapting to an actor already AT its own bridge command still re-teaches the crouch once the actor unfreezes or once the curriculum advances past the bridge rungs -- which would mean the frozen-critic-D/reward wiring itself (not command distance, not actor competence, not LR) is the wall, closing the whole actor-init correction class and pointing back at the critic pretraining objective. Strongest alternative: the bridge ignites fine but the SAME collapse recurs the moment V2's heading/DR rungs take over -- informative either way, and would isolate the failure to multi-direction generalization rather than acquisition per se.

**gate**: Pre-registered by operator fb_20260818T102844_116d4c item 5: bridge B0 must promote by <=1M steps; at the 4M canary budget require frontier>=B2, cmd_prog_frac>=0.6, height_factor>=0.8, slip_per_m<=2.0, zero falls in the final cert round (stricter than the earlier tournament's B0-only bar, since this arm starts already walking). Gate-eval must confirm behaviorally: DR-0 det gait_valid>=4/6, no static-crouch video, all 6 legs cycling. FAIL on this pre-registered bar closes the actor-init correction class for good (per STATUS.md: 'one evidence-based correction to the critic-D recipe, not a same-recipe re-run') and points at the frozen-critic-D/reward wiring itself as the wall -- no further same-shape re-run without a new hypothesis. PASS licenses the full 40M cw-dynrep-criticD-walkcurr4 with this exact recipe.

**refused_reason**: a process for cw-dynrep-criticD-walkcurr4-bridge1 already exists on hexapod-mjx-train-11

**note**: STALE launch-attempt stub — the real run executed on hexapod-mjx-train-11 (W&B ytfh9o3j, CRASHED_BUG at 2.03M, recovered as bridge1-r1 per operator order fb 20260818T111051Z); reconciled 2026-08-18T11:27Z.

