# cw-recover-any15-retentionrollback-cont1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-17T19:11:25+00:00

**pod**: hexapod-mjx-train-0

**steps**: 40000000

**parent**: cw-recover-any11-rsi-scratch1

**wandb_id**: xqcqvb3u

**hardware_ready**: False

**hypothesis**: Make the fallen robot's get-up training self-healing: every time the curriculum promotes to a harder starting position it now saves a full checkpoint of that moment, and if the robot later FORGETS an already-mastered position (same retained bucket below 0.60 for 4M steps of certification time), training automatically rolls back to the last promotion checkpoint instead of grinding on with a damaged policy. This continues the operator-selected best recovery policy (canonical ppo_goal_cw_recover_any11_rsi_scratch1.zip) with its proven any11 recipe INCLUDING goal.recover_rsi_frac=0.5, default 0.50/0.25/0.15/0.10 replay mix plus the new default 0.10 training-error overlay, retention-gated promotion (every promotion requires a fresh same-round pass of all easier buckets >=0.8), full retention suite every 2 cert rounds, 1M cert cadence / 16 cert envs / 40M budget as in any14. Implementation at exact main SHA 7d39a25 (Checkpoint and roll back recovery promotions), 45 recovery tests green. Direct operator order (MCP lane, GPT-5 Codex relaying Lukas's checkpoint/rollback amendment, 20260817T190903Z); supersedes the SIM SPRINT no-new-launch banner for this one run. Prediction-if-true: ladder climbs with every CERT/recover_promoted=1 paired with recover_retention_suite_passed=1, promotion checkpoints appear under policies/recover_promotions and on W&B, and any retention collapse triggers a logged rollback that restores the frontier instead of a permanent regression. Prediction-if-false: promotions stall behind the retention gate at the same bucket any11 plateaued, or rollback thrashes (repeated restore/regress loop) without net ladder progress. Strongest alternative: forgetting is caused by the replay mix itself, in which case rollback merely oscillates and the fix belongs in replay weighting, not checkpointing.

**gate**: PASS requires the guard mechanism proven live: (1) at first promotion, RECOVER_GUARD/promotion_checkpoint_saved fires with count/latest_checkpoint_* metrics AND a unique policy ZIP + curriculum JSON exist under policies/recover_promotions and are uploaded to W&B; (2) every CERT/recover_promoted=1 coincides with CERT/recover_retention_suite_passed=1 in the same round; CERT/recover_retention_suite_* logged on the every-2-rounds cadence; (3) adaptive training-error priority/sample-probability metrics (RECOVER_SCORE/bucket_*_training_error_*) respond to fumbled buckets; (4) if any retained bucket stays <0.60 for 4M env steps, rollback fires: pending then applied/target/trigger metrics, policy+optimizer+curriculum restored before next rollout with training-error debt retained. Behavioral: ladder frontier >= any11's without any unretained promotion; video-verify any earned frontier (no flag/stilt/park).

**verdict**: FAIL against the gate as written (behavioral clause: frontier B8 < any11's B15) — but the dig-in shows the CLAUSE was invalid, not the run. MECHANISM PASS on gate items (1)-(3): 8 promotions B1@1.05M..B8@25.03M, each with a saved ZIP + curriculum JSON on pod and W&B (promotion_checkpoint_count=8, latest_checkpoint_bucket=8), every CERT/recover_promoted=1 round paired with retention_suite_attempted=1 AND passed=1, full-suite cadence honored, and RECOVER_SCORE training-error priority responded to fumbles (b4 0.00 -> 0.20, sample_probability re-weighted 0.45 -> 0.06-0.26). Item (4) UNTESTED and MIS-TARGETED: rollback never fired because max_regression_age_steps peaked at 3.01M < the 4M trigger — the real failure mode is OSCILLATION (a bucket reads 0.00 then 1.00 in adjacent 1M cert rounds, resetting the consecutive-failure timer), not the monotone forgetting the timer was designed for. ROOT CAUSE of the stall: no forgetting, no erosion. Over the last 15M steps retained buckets 0-3 pass 100% of rounds, b4 0.85, b5/b6 0.90; the wall is the frontier neighborhood itself — b7 crouch_deep (pass 0.47, mean 0.59) and b8 partial_high (pass 0.14, mean 0.31). Promotion was blocked at 28M and 34M with promotion_candidate=1 by b7 dips in the SAME round: the conjunctive one-shot bar correctly refused to promote a policy that cannot hold crouch_deep and partial_high simultaneously. Conversely any11's B15 was earned under rotating-subset certification with stale passes — its own history shows b1=0.06/b2=0.00/b3=0.00 at 15M while its frontier read 13, and b0=0.06/b1=0.31 at 38M with frontier 15. MEASUREMENT CORRECTION (the run's real contribution): matched one-shot gate evals put the two policies at EQUAL capability — det 10/18 at DR-0 for both, det 11/18 at DR-0.1 for both, sto 0/18 for both — so "B15 vs B8" is a certification-standard artifact, not a capability gap, and this lineage's honest same-round-certified frontier is B8. Composition shifted (any15 gained plant_catch/onefoot_micro/onefoot_mid, lost partial_high/zero/tangle_mild at DR-0). Reward decline -116.9 -> -125.9 is curriculum composition as the frontier climbed 0->8, not degradation. Video honest: successes are genuine six-foot recoveries (no flag/stilt/park; tangle_mid and bank strips stand up cleanly), and b8 failures are NOT falls — the robot ends upright, roll_tail 0.1 deg, end_posture_ok True, valid_plant True, but never earns held-success (plant_margin 128.8mm vs 143.3mm on a success): "stands but not certifiably". Warm-start also does not carry curriculum state (frontier_before=0 at the first round) — a real design gap, not the cause here. No follow-up arm: recover/tangle redesign stays operator-gated and is outside the SIM SPRINT. If the line reopens: make the rollback trigger watch a windowed pass RATE instead of consecutive sub-threshold age, and put the first lever on crouch_deep/partial_high stability, not on more ladder.

