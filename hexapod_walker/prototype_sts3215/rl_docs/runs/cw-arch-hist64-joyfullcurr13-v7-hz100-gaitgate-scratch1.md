# cw-arch-hist64-joyfullcurr13-v7-hz100-gaitgate-scratch1

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-25T02:16:24+00:00

**pod**: hexapod-mjx-train-9

**steps**: 40000000

**wandb_id**: hvyjdmch

**hypothesis**: Plain English: does training the V7/100Hz/hist64 recipe FROM SCRATCH with the already-built anti-leg-sacrifice reward gate (reward.walk_gait_gate=1.0) turned on from step 0 avoid ever forming the {0,2,5}-leg-sacrifice basin that the identical from-scratch recipe without the gate (cw-arch-hist64-joyfullcurr13-v7-hz100-scratch-s0-r1) converged to and FAILed on? walk_gait_gate multiplicatively collapses walk/progress/tracking income toward zero whenever any support leg hasn't completed a real swing within a 2s window -- purpose-built+bank-tested (test_walk_gait_gate_*, quadwalk precedent) but never included in this recipe until the sibling continuation cw-arch-hist64-joyfullcurr13-v7-hz100-gaitgate-cont1 (a warm-start rescue of the already-collapsed scratch-s0-r1 checkpoint, owned by another cycle, still training). This arm is the from-scratch half of that same question: prevention instead of rescue, decoupled from whether the collapsed basin is too deep to escape by reward alone.

**gate**: PASS: DR-0 det gait_valid 6/6, no leg duty pinned near-zero (no [X,Y,Z]-style sacrifice), walkcurr/frontier promotes past b0. PARTIAL: gait_valid improves over the no-gate parent's 0/6 but some leg sacrifice or frontier stall remains. FAIL: identical {0,2,5} (or any 3+ leg) sacrifice signature reproduces despite the gate being active from step 0 -- would mean walk_gait_gate's income collapse is too weak/late to prevent the basin, not just too weak to rescue from it, and the leg-sacrifice root cause is NOT reward-side at all (points at the still-unclaimed structural/indexing DIG-IN, OPERATOR_QUESTIONS.md 08-24/08-25 leg-sacrifice notes).

**verdict**: Result: FAIL -- baking reward.walk_gait_gate in from step 0 does NOT prevent the leg-sacrifice-then-fall basin; it is WORSE than the ungated parent, not better. Evidence: DR-0 gate 0/6 pass on every sub-mode (walk det+sto, walk_startjitter det+sto), term_reason=over_current on every episode (Imax 2.64-2.70A), duty_cycle [1.0,0.07,1.0,0.05,1.0,0.05] (3 legs permanently locked stance, 3 legs almost never touch down) -- a rigid tripod-lock, not gait cycling; video (gate+owncfg contact sheets) shows the robot rearing nose-up onto stiff planted legs with the others splayed, never six-leg cycling. Held-out 60s joygate is WORSE than the ungated parent: falls 39/48 (parent 12/48), gait_valid_frac 0.146 (parent 0.0), per-leg duty [1.0,0.04,0.97,0.17,0.99,0.13] -- legs 1/3/5 sacrificed, matching the pre-registered FAIL branch verbatim. Own-DR pass: same collapse, slip/m up to 11.86, roll peak 20deg. Why: jointly with the concurrent cycles gaitgate-cont1 FAIL (continuation), this is 2/2 -- reward-side gating (continuation OR from-scratch) cannot prevent/dislodge this basin; root cause is not simply "gate absent," closing that lever. CORRECTION found this cycle: this run and its stated parent (scratch-s0-r1) are NOT byte-identical except for the gate as claimed -- scratch-s0-r1 trained pre-08-24-mesh-flip (implicitly primitive-family, its own failure is tilt_pitch topple, near-zero over_current) while this run (created 08-25, no env.model_source override) silently defaulted to MESH (confirmed: over_current-dominated, zero tilt_pitch, Imax pinned ~2.64-2.70A matching the established mesh over_current signature) -- so it answers "does the gate prevent the MESH basin" (no), not the intended primitive-family question, and gaitgate-cont1 likely has the same undisclosed confound (a primitive checkpoint resumed with no override, silently trained on mesh physics). Built+shipped a fleet-wide fix this cycle: launch_run.py now REFUSES any --parent launch whose resolved env.model_source mismatches the parents own lineage-implied family (mirrors the control.hz enforcement; --allow-model-source-mismatch escape hatch), so this cannot recur silently -- rl_move/tests/test_launch_run_model_source_continuity.py, 16/16 green, snapshotted (tag exp/model-source-continuity-guard). What next: the mesh leg-sacrifice/over_current basin needs a walk-tick current-dwell charge (reward.k_walk_current, analogous to the already-proven reward.k_walk_stop_current for stop-ticks) since the min-across-legs gait gate is gameable (brief token swings + otherwise-rigid lock) and actually less safe than no gate; any further resume of this lineage must now pass an explicit env.model_source.

