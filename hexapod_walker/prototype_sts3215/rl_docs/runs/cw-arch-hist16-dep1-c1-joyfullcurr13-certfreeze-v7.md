# cw-arch-hist16-dep1-c1-joyfullcurr13-certfreeze-v7

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-24T17:29:10+00:00

**pod**: hexapod-mjx-train-1

**steps**: 40000000

**parent**: cw-arch-hist16-dep1-c1-joyfullcurr9-stopcur2

**wandb_id**: flziz48k

**hypothesis**: Plain English: certfreeze (cert-only freeze, training freeze OFF) proved the freeze mechanism itself is not the joygate's over_current-fall culprit -- the b2+ heading-widening PRACTICE DIET is (3/3 arms: full training freeze, cert-only freeze, both current-charge doses, same ~6-7/48 regression). The plain V6 bucket sampler never draws in-place turning or a full reversal in any bucket -- the held-out joygate's stress_mix family (sweep_circle/square/flip_180) draws both every episode. This single-lever swap (identical recipe to certfreeze -- same stopcur2 warm-start, same k_walk_stop_current=2.0, same cert-only-freeze frontier assist -- only --walk-curriculum-version 6 -> 7) adds wz in-place turning (+-0.3 rad/s, 50% zero) and a 15% full-reversal chance to every non-bridge bucket, so training practices the distribution shape the joygate actually tests. Also doubles as evidence on the NEW leg-3-lock-at-DR-0 finding from certfreeze's own gate (a lock this diet-diversification MIGHT relieve if it is a general under-practice symptom, or MIGHT leave untouched if it is specifically driven by the wide heading BAND itself rather than command diversity within it).

**gate**: PASS: frontier still promotes past b1 (cert-only freeze keeps working) AND held-out 60s joygate falls <=2/48 (parent stopcur2 band) AND DR-0 gate det gait_valid stays >=5/6 with no leg-3 sacrifice (certfreeze's own new regression). PARTIAL: joygate falls improve (e.g. 3-4/48) without clearing 2/48, or frontier/DR-0-gait regress while joygate clears -- a genuine trade, not a clean win. FAIL: joygate stays >=4/48 over_current (diet diversification within-band does not touch the failure) and/or leg-3 lock persists unchanged -- points the next lever at the heading-BAND-WIDTH itself (a narrower or staged band-opening ladder), not command diversity.

**verdict**: Result: FAIL, worse than parent v6 -- the frontier never left bucket b1 for the entire 40M-step run, so V7's intended wide-heading stress-diet test never happened. Evidence: walkcurr/promotions=1 (only the initial b0->b1 promotion at cert_round 1); pre_b1_pass=False in every one of the remaining 79/80 cert rounds; b1 (front45_20s) own stop_speed_m_s pinned at 0.020-0.033 vs the 0.015 cert bar the whole run, never trending down. Reward quarters fell 694->641->525->438 (declining, not rising) -- per the 08-21 ruling this is a genuine stuck/regressing signal, not misalignment-with-rising-reward. Held-out joygate (already run): FAIL, falls 3/48 (cap 2/48), dir_err med 51.9deg (allow 40), gait_valid_frac 0.9375. DR-0 gate: det 6/6 clean but noisy progress (med 0.67, range 0.57-1.81); sto 5/6 with a full leg-0 sacrifice + near-total stall (prog 0.02, slip 55, video: robot parked marching almost in place). own-DR(0.5): det 5/6 with a 2-leg [3,4] sacrifice+stall (video: two legs held rigid while body wiggles), sto clean 6/6. Why: root-caused via training-side telemetry, not guesswork -- WALKCURR_BUCKETS_V7's own banner blames 'the b2+ heading-widening practice diet' but its code applies the new wz-turn/reversal diet 'from front45_20s onward', which INCLUDES front45_20s/60s -- still front-cone, not widened-heading, and the exact rung (b1) that must keep certifying for the frontier to ever advance. Baking a full in-episode reversal into the same 20s window that must also settle to a stop leaves residual momentum the 0.015 m/s stop-gate can't absorb, so b1 can never re-certify once the diet lands there -- a scope bug in the fix, not evidence against the diet-diversification hypothesis itself (which V6 already proved gets past b1-b5 cleanly with a plain diet). What's next: built+tested+snapshotted WALKCURR_BUCKETS_V8 (walk_task.py) -- byte-identical to V7 except the stress-diet extras (wz_max=0.3, reversal_frac=0.15) now start at side90_20s (the first genuinely widened-heading rung) instead of front45_20s, so bridge_10s/front45_20s/front45_60s stay bit-exact V6 (verified: test_v8_front45_sampler_bit_exact_with_v6). test_walk_curriculum.py 48/48 (+5 new V8 tests), test_walkcurr_mjx.py 19/19, targeted walkcurr/walk_progress semantics green (reward code untouched by this change). Launching cw-arch-hist16-dep1-c1-joyfullcurr14-certfreeze-v8 same-cycle: identical recipe to certfreeze-v7 (same stopcur2 warm start, same k_walk_stop_current=2.0, same cert-only-freeze) with --walk-curriculum-version 8 as the single lever.

