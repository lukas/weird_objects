# cw-standwalk-stance-mesh2-stage2-dualbc1-anchor5-stdmild2

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: CANARY FAIL - MECHANISM

**created**: 2026-08-27T00:36:04+00:00

**pod**: hexapod-mjx-train-1

**steps**: 2000000

**parent**: cw-standwalk-stance-mesh2-stage2-dualbc1-anchor4-stdanneal

**wandb_id**: qggsdmzc

**hypothesis**: Plain sentence: the shared-log_std anneal from anchor4-stdanneal fixed stochastic hold (6/6->0-2/6 term) but destroyed walk into an anchor1-class leg-sacrifice freeze on both seeds -- this arm tests whether a MILDER final noise target (-2.0 instead of -4.0, i.e. std 0.135 instead of 0.018, same anneal-frac 0.5) keeps enough exploration noise on walk's still-fragile core (weak crawl, prog 0.3-0.4, far from converged) to avoid the degenerate freeze, while still meaningfully cooling the stochastic-hold failure. Single lever vs anchor4-stdanneal: --log-std-final -4.0 -> -2.0, everything else (coef=3.0, isolate_update=1) unchanged. Prediction-if-true: walk stays gait_valid >=5/6 det (no anchor1/anchor4-class catastrophe) while hold/sto term meaningfully improves vs the anchor2/3 6/6 baseline (need not hit the full <=2/6 bar). Prediction-if-false: walk still collapses even at this milder dose -- confirms noise MAGNITUDE isn't the tunable knob and the real fix needs the harder per-core log_std split (DualGruActorCriticPolicy currently shares one log_std across both gated cores; already-flagged DIG-IN).

**gate**: MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition, close a behavior/reward class, or require mature gait at this checkpoint. MECHANISM-HEALTH CANARY ONLY: do not judge skill acquisition or require mature gait. Same joint hold/rise/lower/walk DR-0 det+sto + own-DR(0.5) panel as anchor2/anchor3/anchor4-stdanneal. WALK-SURVIVES if det gait_valid >=5/6 on BOTH seeds (no anchor1/anchor4-class 3+-leg-sacrifice freeze, prog_ratio >=~0.2). HOLD-HELPS if hold/sto DR-0 termination drops meaningfully from the anchor2/3 6/6 baseline (any improvement counts partial credit; <=2/6 is the full bar) on at least one seed. FULL PASS (promote this dose) = WALK-SURVIVES both seeds AND HOLD-HELPS at least partial on both. FAIL if walk still shows the anchor4-class catastrophe on either seed at this dose (confirms magnitude isn't the lever -> per-core log_std split is the only remaining lever) or if hold shows zero improvement (confirms anneal isn't doing anything at this dose).

**verdict**: CANARY FAIL - MECHANISM (hold-helps branch, own-scope, single seed). Milder log_std anneal (-2.0, std=0.135) protects walk but does NOTHING for hold's stochastic termination -- the gate's own explicit zero-improvement FAIL branch fires. Plain English: half of the hoped-for trade (protect walk) came true, the other half (cool hold) did not, at this specific dose. Evidence (own report.json, DR-0 + own-DR(0.5), det+sto, video-watched): WALK-SURVIVES holds on this seed -- det gait_valid 5/6 DR-0 / 6/6 own-DR (only 1-leg sacrifice on one DR-0 episode, no anchor4-class 3+-leg freeze), prog_ratio med 0.26 DR-0 / 0.29 own-DR (matches anchor2/3's healthy 0.29-0.43 band), video confirms real cycling legs and slow net translation (fwd 0.64-0.69m/30s), not a static splayed pose -- an earlier 3-frame video skim mistakenly read this as frozen; the full contact sheet shows the 'feet' overlay pattern actually changing across the episode. But HOLD-HELPS FAILS outright: hold/sto stays 6/6 hold_min_load termination at BOTH DR-0 and own-DR -- bit-for-bit the anchor2/3 unchanged baseline, zero movement -- and hold/det, which was clean (0/6 term) in anchor2/3, picked up NEW terminations (2/6 DR-0, 4/6 own-DR), a mild regression, not even neutral. Video of a terminated hold episode (hold_det_3) shows a stable-looking planted crouch, not a visible collapse -- the min_load trip is a load-threshold sensor event, not a visual failure. Why: std=0.135 at this dose is apparently still close enough to the walk core's own natural trained noise to leave walk's exploration basin undisturbed (good) but ALSO too close to make any dent in whatever exploration-noise-dependent mechanism let hold settle into its low-height/min-load failure mode (bad) -- unlike anchor4's -4.0 (std=0.018) which visibly cooled hold (2/6, 0/6 sto) but destroyed walk. Next: this is one of a pre-registered 2-dose x 2-seed grid (stdmild1 -1.0, stdmild2 -2.0, both seeds) testing anneal MAGNITUDE as the safety knob; my own single-seed read here already shows -2.0 alone is not the answer (helps nothing on hold) even though it's safe for walk -- the joint call (folding in stdmild1's -1.0 seed0 and both seeds' -s1 twins once they land) will decide whether ANY point on this magnitude axis threads the needle, or whether the already-flagged per-core log_std split (DualGruActorCriticPolicy currently shares one log_std across both gated dual-cores) is the only remaining lever. Own eval prestage timed out at 55min (100Hz/30s-episode/video-every-1 x 48+ episodes exceeds the watcher's fixed 3300s wrapper budget -- logged, not a mechanism finding) so gate+owncfg were pulled and read manually this cycle.

