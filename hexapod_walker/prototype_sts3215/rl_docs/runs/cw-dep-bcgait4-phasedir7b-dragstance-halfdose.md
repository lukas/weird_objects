# cw-dep-bcgait4-phasedir7b-dragstance-halfdose

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-22T08:17:32+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-dep-bcgait4-phasedir7-dragstance

**wandb_id**: c6ygx01j

**hardware_ready**: False

**hypothesis**: The phasedir7 policy proved reward.k_drag_stance has real, measurable bite (det slip/m improved 3.04->2.50, ratio 1.611x->1.324x clone) but at k=8000 it overcorrected: progress dropped 0.961x->0.779x clone (cap 0.9x, a new miss) and speed fell to 0.059 m/s (just under the [0.06,0.096] floor, also a new miss), with env/reward_drag_stance sitting flat around -20 to -25 the WHOLE 2M-step run (never decaying) while training reward fell monotonically the entire run (-5 -> -7512) -- a dose-too-strong signature, not an undertrained one. This is the pre-registered branch (ii) from phasedir7's own gate: "mechanism intact, dose wrong -- retry at k=4000 (half dose)." Identical to phasedir7 in every other respect (same warm start, same --warm-log-std-override -2.0, same course/anchor/height/loadslip-ratio stack, same 2M budget, seed 13) with ONLY reward.k_drag_stance changed 8000.0 -> 4000.0 (allow_mm=6.0 and tick_floor_mm=0.25 unchanged). Prediction-if-true: progress and speed recover back toward phasedir6's PASS range (>=0.9x clone, >=0.06 m/s) while slip/m stays meaningfully better than phasedir6's 1.611x (even if not all the way to <=1.15x at half dose) -- i.e. a real point on the dose-response curve between phasedir6 (k=0 effectively, slip 1.611x/progress 0.961x) and phasedir7 (k=8000, slip 1.324x/progress 0.779x). Prediction-if-false-(i): slip regresses most of the way back toward phasedir6's 1.611x while progress/speed recover -- the dose-response is steep/near-binary between 0 and 8000; try an intermediate rung (k=6000) or consider a floor-based (not linear) charge shape. Prediction-if-false-(ii): progress/speed STILL miss at half dose -- the charge magnitude is not the limiting factor, something else in the stack (course/anchor pricing) is now the binding constraint at ANY k_drag_stance > 0; stop dosing this lever and dig into why the course reward can't recover speed under ANY additional structural charge. Prediction-if-false-(iii): slip/m improves further (better than phasedir7's 2.50) even at half dose while progress/speed also recover -- the two axes are less coupled than phasedir7 suggested; run rung B (heading-set respec) directly from this checkpoint.

**gate**: At 2M, DR-0, forward panel (same eval_checkpoint det+sto invocation and cfg-sets as phasedir7's gate) on the final checkpoint, clone-relative against the SAME frozen control (logs/ckpt_eval/phasedir3_clone_control_gate -- do not re-run it). PASS requires ALL: (a) zero falls, gait_valid 6/6; (b) progress >= 0.9x clone; (c) slip/m <= 1.15x clone; (d) dir_err med <= clone + 5deg; (e) speed_mean in [0.06,0.096]. VERDICT MUST additionally report: policy_std (expect ~0.13), W&B env/reward_drag_stance trajectory (compare its steady-state magnitude to phasedir7's flat -20/-25 -- if it is meaningfully smaller in magnitude AND still not decaying, that's consistent with "half the dose, same non-learned behavior", informative either way) and a direct 3-point dose-response table (phasedir6 k=0-equiv, phasedir7 k=8000, phasedir7b k=4000) for progress/slip/speed in the verdict. Per-leg check: swing count/leg, stride_m_mean, duty skew across all 6 legs vs both phasedir6 and phasedir7's numbers (see joystick STATUS.md Now section) -- report whether phasedir7b sits BETWEEN phasedir6 and phasedir7 on these axes (expected if the dose-response is smooth) or off that line (would flag a different/nonlinear mechanism). PASS -> pre-registered rung B respec: goal.walk_heading_set=[0,0.7854,-0.7854], same gate per heading bin. FAIL branch (i) slip regresses toward phasedir6, progress/speed recover -> try an intermediate rung (k=6000) or a floor-shaped (non-linear) charge, single change. FAIL branch (ii) progress/speed still miss at half dose -> STOP dosing k_drag_stance; the binding constraint is elsewhere in the stack (course/anchor pricing) -- dig-in required before any further k_drag_stance value. FAIL branch (iii) slip improves further AND progress/speed recover -> not a fail, proceed to rung B directly from this checkpoint. NO DOWNLOAD_ANSWER change from this run.

**verdict**: FAIL, matches its own pre-registered branch (ii): halving k_drag_stance 8000->4000 did NOT recover progress/speed and did NOT improve slip -- confirms the dose magnitude is not the limiting factor. det progress med 0.616 vs clone 0.766 = 0.804x clone (cap 0.9x, barely moved from phasedir7's 0.779x); det slip/m med 2.542 vs clone 1.889 = 1.346x clone (cap 1.15x, actually a hair WORSE than phasedir7's 1.323x -- halving the charge did not help slip either); det speed_mean 0.0585 m/s, still under the 0.06 floor (same miss as phasedir7's 0.059). dir_err 29.0deg vs clone+5=40.5deg cap PASS. Zero falls, gait_valid 6/6, DR-0+own-DR det+sto. policy_std 0.133 (override intact). Per-leg signature is ESSENTIALLY IDENTICAL to phasedir7 despite the 2x dose difference: swing 19-23/leg (phasedir7: 19-24), duty skew 0.42-0.56 (phasedir7: 0.43-0.55), stride 0.036-0.039m (phasedir7: 0.036-0.038m) -- the policy did not change its gait AT ALL between doses. W&B confirms: env/reward_drag_stance sat flat around -10 to -11/step the whole 2M-step run (phasedir7 was flat -20 to -25, i.e. exactly proportional to the halved k, meaning the SAME fraction of stances exceed the 6mm allowance regardless of dose) and env/walk_loadslip_ratio stayed 4.2-5.0 (unchanged from phasedir6/7). ep_rew_mean plateaued around -3800 (half of phasedir7's -7500, again exactly proportional) after ep_len_mean saturated at 375 by ~800k steps -- not a rising-reward case. 3-POINT DOSE-RESPONSE TABLE: phasedir6 (k=0-equiv): progress 0.961x, slip 1.611x, speed pass. phasedir7 (k=8000): progress 0.779x, slip 1.323x, speed 0.059 FAIL. phasedir7b (k=4000): progress 0.804x, slip 1.346x, speed 0.0585 FAIL. Progress/slip/speed are FLAT between k=4000 and k=8000 while both differ sharply from k=0 -- this is a STEP FUNCTION (any k_drag_stance>0 triggers the same gait-compression response and speed-floor breach), not a smooth dose-response curve. Matches gate branch (ii) exactly: "progress/speed still miss at half dose -> the charge magnitude is not the limiting factor, something else in the stack (course/anchor pricing) is now the binding constraint at ANY k_drag_stance > 0; stop dosing this lever." ACTED ON: stopping the k_drag_stance dose search. DIG-IN required before any further reward edit on this lineage -- see logline/final message.

