# cw-dep-bcgait4-phasedir7-dragstance

<!-- GENERATED from experiments.json by launch_run.py — do not edit -->

**status**: FAIL

**created**: 2026-08-22T08:02:08+00:00

**pod**: hexapod-mjx-train-0

**steps**: 2000000

**parent**: cw-dep-bcgait4-phasedir6-slipband

**wandb_id**: ysb2v3ak

**hardware_ready**: False

**hypothesis**: The phasedir6 policy still finances progress with the same duty-skewed dragging-leg gait as phasedir5, because the episode-cumulative loadslip RATIO pricing (walk_loadslip_gate/k_loadslip_excess) is structurally dominated by the anchor/course/height reward terms at any (ok,max) band value -- confirmed this cycle: retightening 7/10->3/6 raised the charge collected (walk_loadslip_factor 0.99->0.48-0.56) but left the ratio itself unmoved (4.2-4.7 all run) and det slip WORSE (1.611x vs 1.590x clone). Rather than dial a third band value (refuted lever, per phasedir6's own pre-registered fork), this arm switches PRICING MECHANISM to one already built and bank-tested but never enabled in this lineage: reward.k_drag_stance prices each foot's PER-STANCE accumulated drag directly in mm (not diluted by episode-length progress the ratio is), calibrated 2026-08-11 (test_drag_stance_stack_prices_skating_below_stepping, k=8000/m over a 6mm allowance, 0.25mm/tick jitter floor) to make a learned skater's drag cost ~2.4x its income while an honest scripted gait pays on <5% of stances. Identical to phasedir6 in every other respect (same warm start, same --warm-log-std-override -2.0, same course/anchor/height/loadslip-ratio stack UNCHANGED, same 2M budget, seed 13) PLUS reward.k_drag_stance=8000.0, reward.drag_stance_allow_mm=6.0, reward.drag_stance_tick_floor_mm=0.25 -- a single additive mechanism, nothing removed. Validated by a matched-env pod pricing A/B before spending budget (--evidence logs/ckpt_eval/pd7_dragstance_ab_{clone,drag}): under the full phasedir6 stack + only this new charge, the honest phase1 BC clone (det slip/m 1.89) scores -1868 vs the phasedir6 cheat checkpoint (det slip/m 3.04) scoring -3971 -- a >2x swing in favor of the honest gait, reproducing the original audit's calibration claim on the ACTUAL cheat checkpoint (not just a scripted twin). Prediction-if-true: W&B reward_drag_stance trends toward 0 (foot stances staying under the 6mm allowance) while training reward improves relative to phasedir6's trajectory, and the det gate lands slip <=1.15x clone with progress still >=0.9x. Prediction-if-false-(i): reward_drag_stance stays large and negative all run with progress/reward still rising -- the charge is correctly priced but 2M steps is not enough for PPO to find the alternative gait; continue from checkpoint per the 08-21 ruling, do not re-verdict as a fresh fail. Prediction-if-false-(ii): progress craters / idle-park from step 0 -- k=8000 is too aggressive for this warm start; retry at k=4000 (half dose) as phasedir7b, single change. Prediction-if-false-(iii): reward_drag_stance falls to near zero (mechanism bites) but det slip STILL misses the clone band -- the harness's slip_per_m metric and the env's per-stance mm metric diverge; next step is reconciling those two measurements, not another coefficient.

**gate**: At 2M, DR-0, forward panel (same eval_checkpoint det+sto invocation and cfg-sets as phasedir6's gate) on the final checkpoint, clone-relative against the SAME frozen control (logs/ckpt_eval/phasedir3_clone_control_gate -- do not re-run it). PASS requires ALL: (a) zero falls, gait_valid 6/6; (b) progress >= 0.9x clone; (c) slip/m <= 1.15x clone; (d) dir_err med <= clone + 5deg; (e) speed_mean in [0.06,0.096]. VERDICT MUST additionally report: policy_std (expect ~0.13, override unchanged), W&B env/reward_drag_stance trajectory (must trend toward 0 -- if it stays large/negative all run with progress+reward still rising, per the 08-21 ruling this is CONTINUE, not FAIL) and env/walk_loadslip_ratio (informational only -- the mechanism under test is the stance charge, not this ratio). Per-leg check in verdict: swing count/leg (clone ~30, phasedir5/6 cheat ~19-25), stride_m_mean (clone ~0.034, cheat ~0.0435), duty skew across all 6 legs (clone uniform 0.50-0.54, cheat skewed idx1/3/5 ~0.54 vs idx0/2/4 ~0.44-0.49) -- PASS should show these moving back toward the clone cluster, not just the aggregate slip/m number improving. PASS -> pre-registered rung B respec: goal.walk_heading_set=[0,0.7854,-0.7854], same gate per heading bin. FAIL branch (i) charge active + progress/reward still rising -> per RUN_INTERPRETATION_RULES continue from checkpoint, do not verdict FAIL. FAIL branch (ii) progress collapse/idle from early steps -> phasedir7b at k_drag_stance=4000 (half dose), single change. FAIL branch (iii) charge bites (reward_drag_stance ~0) but det slip/m still misses clone band -> name the env-metric/harness-metric divergence explicitly and reconcile the two measurements before any further reward edit. NO DOWNLOAD_ANSWER change from this run.

**verdict**: FAIL -- pre-registered branch (ii) fired ("mechanism intact, dose wrong"): k_drag_stance=8000 over-corrected. Zero falls DR-0+own-DR det+sto, gait_valid 6/6 all four passes (safety fully preserved) -- but THREE of five gate axes miss where phasedir6 missed only one: (b) progress 0.597 vs clone 0.766 = 0.779x clone (cap 0.9x, a real regression from phasedir6's 0.961x PASS); (c) slip/m 2.50 vs clone 1.89 = 1.324x clone (cap 1.15x, still a FAIL but a genuine IMPROVEMENT from phasedir6's 1.611x -- the mechanism has real bite, confirming the diagnosis); (e) speed_mean 0.059 m/s, just under the [0.06,0.096] floor (phasedir6 passed easily at 0.067). (d) dir_err 30.8deg vs clone+5=40.5 cap still PASSES. policy_std held 0.134 (override intact). W&B env/reward_drag_stance sat essentially CONSTANT (-19.6 to -25.5) for the entire 2M-step run with no visible downtrend -- unlike a charge the policy learns to avoid, this one stayed fully "priced in" the whole run, and env/ep_rew_mean (rollout/ep_rew_mean) fell MONOTONICALLY throughout training (-5 -> -7512 at the final step, no plateau/recovery) -- per RUN_INTERPRETATION_RULES this is NOT the "bad evals + reward still rising, so continue" case; reward was falling the entire run, consistent with a dose too strong for this warm start/budget rather than an undertrained run. Per-leg: swing/leg now 19-23 (tighter cluster, was 19-25), stride_m_mean improved to 0.037 (clone 0.0335, was 0.0435) -- moving toward the clone -- but duty skew (idx1/3/5 ~0.52-0.54 vs idx0/2/4 ~0.43-0.50) is essentially UNCHANGED from phasedir6, and overall speed/progress undershoot the command band, reading as an overly cautious/slowed gait rather than a cleaner one. Video (contact sheet + 3 det clips) shows a normal-looking alternating-tripod walk, no new visual exploit. ACTED ON per this run's own pre-registered branch (ii): launched cw-dep-bcgait4-phasedir7b-dragstance-halfdose (train-0, single change: k_drag_stance 8000.0 -> 4000.0, drag_stance_allow_mm/tick_floor unchanged) to find the dose that keeps the slip improvement without paying it in progress/speed.

