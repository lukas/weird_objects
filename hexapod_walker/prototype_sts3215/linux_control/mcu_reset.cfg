# remoteocd recipe: reset-only (no flash). Boots the flashed sketch NOW.
# After an SoC reboot the STM32 does not auto-run; stock arduino-router
# brings it up ~80 s into boot. remoteocd upload -f mcu_reset.cfg /dev/null
init
reset
shutdown
