- 08-27 10:3x standwalk: anchor7-detachtrunk{,-s1} kick-cycle housekeeping (no verdict) -- WIRING CHECK FIRST satisfied both seeds (grad-isolation unit tests green + cfg_set confirms train.bc_anchor_detach_trunk=1 actually threaded into both runs); long 4-mode joint panel healthy/progressing on fresh (non-stale) pod_eval workers, no orphaned-wrapper risk this time; reward-quarter peek ambiguous (seed0 slightly worse, seed1 slightly better vs own baseline) -- mechanism call awaits the eval; 12/12 pods free, nothing else launchable anywhere, correctly idle
- 08-29 07:4x idle-kick standwalk: coursedisp-c1's own gate/owncfg/mixedsession are genuinely still computing (48-episode gate at ~2.5-4min/episode on a contended pod, hours not the usual <=45min budget, ps-verified alive throughout) -- rather than block the cycle, ran the DIG-IN's --course-trace diagnostic directly against this run's OWN live gate rollout (kubectl exec, killed once enough ticks banked so as not to starve the official passes further): 20,838 commanded walk ticks, mechanism active on 71% (clears the >=50% telemetry bar, confirmed LIVE on this real checkpoint, not just the frozen bank replay). Answered the canary's own delta question directly: windowed net-displacement dir_err reads clean (15.8/4.3 deg mean/median) but the harness's PER-TICK INSTANTANEOUS dir_err over the SAME ticks is flat (60.3/32.4 vs baseline ~55-65deg band) -- PASS-no-delta, not a fail (reward healthy, 2 spot-checked contact sheets show clean six-leg gait). Root-cause of the no-delta: direction_err_mean_deg is an instantaneous per-tick stat that structurally cannot see stride-to-stride sway a windowed/net reward term integrates away by construction -- a dose/window sweep is predicted to fail for the same reason, not worth funding blind. Flagged DIG-IN: is direction_err_mean_deg the right DONE-gate metric for a gait with structural per-stride sway, or does a real per-tick (not windowed) mechanism exist that doesn't blow up slip the way k_walk_cmd_track did. Ledger verdict on coursedisp-c1 itself DEFERRED pending official report.json (gait_valid_frac/terminations not yet in-hand) -- next cycle to see SYNCED should verdict PASS-no-delta-mechanism-confirmed using the official numbers plus this note. hdgset1/long-s0-cont1 mixedsession (train-4) reconfirmed still genuinely computing, untouched. Other tracks unchanged (joystick/amp/cpg DONE-or-maintenance, walkcurr operator-blocked q_20260824T0233Z). Backlog empty; no new arm legal until the DIG-IN's methodological question resolves. CYCLE_WORKED.
